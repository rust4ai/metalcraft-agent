{
  "openapi": "3.1.0",
  "info": {
    "title": "Metalcraft Agent API",
    "description": "The pod's workshop admin API. Manages personas, skills, flows, keys, chats, diagnostics sessions, integrations, and gateway channels. Authenticate with a Bearer token: the static WORKSHOP_API_KEY or a Metalcraft ID token (mck_…) scoped to this pod.",
    "license": {
      "name": "MIT",
      "identifier": "MIT"
    },
    "version": "0.43.0"
  },
  "paths": {
    "/api/v1/agent-packs": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "list_agent_packs",
        "responses": {
          "200": {
            "description": "Installed agent packs"
          }
        }
      }
    },
    "/api/v1/agent-packs/export": {
      "post": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "post_export_agent_pack",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExportAgentPackRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The .agentpack bytes, or a report if `out` was given"
          }
        }
      }
    },
    "/api/v1/agent-packs/inspect": {
      "post": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "post_inspect_agent_pack",
        "parameters": [
          {
            "name": "url",
            "in": "query",
            "description": "Registry URL to download from",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "path",
            "in": "query",
            "description": "Local .agentpack path",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "The .agentpack archive",
          "content": {
            "application/octet-stream": {
              "schema": {
                "type": "array",
                "items": {
                  "type": "integer",
                  "format": "int32",
                  "minimum": 0
                }
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "What installing this would grant",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentPackPreview"
                }
              }
            }
          },
          "400": {
            "description": "Not a valid agent pack",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "502": {
            "description": "The registry could not be reached",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/install": {
      "post": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "post_install_agent_pack",
        "parameters": [
          {
            "name": "path",
            "in": "query",
            "description": "Local .agentpack path; omit to upload the archive as the body",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "The .agentpack archive",
          "content": {
            "application/octet-stream": {
              "schema": {
                "type": "array",
                "items": {
                  "type": "integer",
                  "format": "int32",
                  "minimum": 0
                }
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Install report"
          },
          "400": {
            "description": "Rejected"
          }
        }
      }
    },
    "/api/v1/agent-packs/registries": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "get_agent_pack_registries",
        "responses": {
          "200": {
            "description": "Origins this pod will download an agent pack from",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Registries"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/registries/{name}/connect": {
      "post": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "post_registry_connect",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "A configured registry name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "token_key",
            "in": "query",
            "description": "Key-store entry to draw the bearer from; defaults to METALCRAFT_TOKEN",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Where the connection got to",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Connection"
                }
              }
            }
          },
          "404": {
            "description": "No such registry is configured",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Registries come from AGENT_PACK_REGISTRIES and cannot be edited",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/registries/{name}/disconnect": {
      "post": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "post_registry_disconnect",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "A configured registry name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The registry, now anonymous",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Connection"
                }
              }
            }
          },
          "404": {
            "description": "No such registry is configured",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Registries come from AGENT_PACK_REGISTRIES and cannot be edited",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/registries/{name}/packs/{id}/manifest": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "get_registry_manifest",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "A configured registry name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "description": "The pack's handle on that host",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The pack's manifest, as the host serves it"
          },
          "400": {
            "description": "That is not a usable pack id",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "No such registry, or no such pack on it",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "502": {
            "description": "The host would not serve it",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/registries/{name}/search": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "get_registry_search",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "A configured registry name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "Search text; omit to browse",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "1–100, default 25",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "What that host publishes"
          },
          "404": {
            "description": "No such registry is configured",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "501": {
            "description": "That host is fetch-only and does not offer search",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "502": {
            "description": "The host could not be searched",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/registries/{name}/status": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "get_registry_status",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "A configured registry name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "What this pod is to that registry",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Connection"
                }
              }
            }
          },
          "404": {
            "description": "No such registry is configured",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agent-packs/{id}": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "get_agent_pack",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Agent pack id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The pack's manifest"
          },
          "404": {
            "description": "Not installed"
          }
        }
      },
      "delete": {
        "tags": [
          "agent-packs"
        ],
        "operationId": "delete_agent_pack",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Agent pack id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "force",
            "in": "query",
            "description": "Orphan any saved agents that use it",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Uninstall report"
          },
          "409": {
            "description": "In use"
          }
        }
      }
    },
    "/api/v1/agent-packs/{id}/update": {
      "post": {
        "tags": [
          "agent-packs"
        ],
        "summary": "`POST /api/v1/agent-packs/{id}/update`",
        "description": "Separate from install because the *consequences* are different: install adds an\nagent, update changes agents that already exist and that somebody may be talking\nto right now. The report says what followed — and, crucially, names any agent\nwhose persona or preset the new version withdrew, so the two silent failure modes\nbecome two lines in a dialog.",
        "operationId": "post_update_agent_pack",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "The installed agent pack to update",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "The newer .agentpack, or use ?url=/?ref=/?path=",
          "content": {
            "application/octet-stream": {
              "schema": {
                "type": "array",
                "items": {
                  "type": "integer",
                  "format": "int32",
                  "minimum": 0
                }
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Update report",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UpdateReport"
                }
              }
            }
          },
          "400": {
            "description": "Not installed, older, or invalid"
          }
        }
      }
    },
    "/api/v1/agent-presets": {
      "get": {
        "tags": [
          "agent-presets"
        ],
        "operationId": "list_agent_presets",
        "responses": {
          "200": {
            "description": "Installed agent presets"
          }
        }
      }
    },
    "/api/v1/agent-presets/{slug}": {
      "get": {
        "tags": [
          "agent-presets"
        ],
        "operationId": "get_agent_preset",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Preset slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The preset, with its roster resolved",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PresetDetail"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "agent-presets"
        ],
        "operationId": "put_agent_preset",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Preset slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentPreset"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Saved"
          },
          "409": {
            "description": "Owned by a pack"
          }
        }
      },
      "delete": {
        "tags": [
          "agent-presets"
        ],
        "operationId": "delete_agent_preset",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Preset slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          },
          "404": {
            "description": "Not user-owned"
          }
        }
      }
    },
    "/api/v1/agents/instances": {
      "get": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "list_agent_instances",
        "responses": {
          "200": {
            "description": "Live agents on this pod",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceList"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "post_create_agent_instance",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateInstanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Created"
          }
        }
      }
    },
    "/api/v1/agents/instances/{id}": {
      "get": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "get_agent_instance",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceDetail"
                }
              }
            }
          },
          "404": {
            "description": "Not found"
          }
        }
      },
      "delete": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "delete_agent_instance",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          },
          "404": {
            "description": "Not found"
          }
        }
      },
      "patch": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "patch_agent_instance",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PatchInstanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Updated"
          },
          "404": {
            "description": "Not found"
          }
        }
      }
    },
    "/api/v1/agents/instances/{id}/conversations": {
      "get": {
        "tags": [
          "agent-instances"
        ],
        "summary": "Every conversation this agent has had, newest activity first.",
        "description": "The same list `GET …/instances/{id}` embeds, on its own route — a session list\nreloads whenever a conversation changes, and it has no use for the agent's\nmemory or its schedules, which that endpoint also pays to assemble.",
        "operationId": "list_instance_conversations",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ChatSummary"
                  }
                }
              }
            }
          },
          "404": {
            "description": "No such agent"
          }
        }
      },
      "post": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "post_instance_conversation",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/NewConversationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatSummary"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/agents/instances/{id}/flows": {
      "get": {
        "tags": [
          "agent-instances"
        ],
        "summary": "`GET /api/v1/agents/instances/{id}/flows` — what this agent is scheduled to do.",
        "description": "The same list `GET …/instances/{id}` embeds, on its own route. It is worth its own\nendpoint because it is the question somebody asks right before they decide whether\nto trust a background agent, and answering it should not mean fetching an agent's\nwhole conversation index.",
        "operationId": "get_agent_instance_flows",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Agent instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Flow schedules armed to this agent",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceFlows"
                }
              }
            }
          },
          "404": {
            "description": "No such agent"
          }
        }
      }
    },
    "/api/v1/agents/instances/{id}/memory": {
      "get": {
        "tags": [
          "agent-instances"
        ],
        "operationId": "get_agent_instance_memory",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "What this agent knows, shipped vs learned"
          }
        }
      }
    },
    "/api/v1/agents/instances/{id}/memory/dream": {
      "post": {
        "tags": [
          "agent-instances"
        ],
        "summary": "Consolidate this agent's memory now instead of waiting for tonight.",
        "description": "The same engine the nightly loop runs, with the same caps — an on-demand run\nand a scheduled one leave the store in the same shape. It **blocks for the\nwhole run**, which is several model calls and can be minutes: this is a button\nsomebody presses and watches, not something to poll. Send `{\"stages\":[1,5]}`\nfor the mechanical half, which returns in milliseconds.\n\nAlways 200 with a report. A stage that failed is named in the report rather\nthan turned into a status code, because the stages that succeeded did real\nwork and a 500 would throw that away.",
        "operationId": "post_agent_instance_dream",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Instance id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DreamRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DreamReport"
                }
              }
            }
          },
          "400": {
            "description": "No valid stages requested"
          },
          "404": {
            "description": "No such agent"
          }
        }
      }
    },
    "/api/v1/api-tools": {
      "get": {
        "tags": [
          "api-tools"
        ],
        "operationId": "list_api_tools",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ApiToolSummary"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/api-tools/{name}": {
      "get": {
        "tags": [
          "api-tools"
        ],
        "operationId": "get_api_tool",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Tool name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HttpApiToolConfig"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "api-tools"
        ],
        "operationId": "put_api_tool",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Tool name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HttpApiToolConfig"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      },
      "delete": {
        "tags": [
          "api-tools"
        ],
        "operationId": "delete_api_tool",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Tool name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/channels": {
      "get": {
        "tags": [
          "gateway"
        ],
        "summary": "List all channels — the built-in `metalcraft` default first, then custom\nchannels. Secrets are never included.",
        "operationId": "list_channels",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Channel"
                  }
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "gateway"
        ],
        "summary": "Add a custom channel (its own gateway url + secret). The `metalcraft` slug is\nreserved for the built-in channel.",
        "operationId": "create_channel",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateChannelRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Channel"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/channels/{slug}": {
      "put": {
        "tags": [
          "gateway"
        ],
        "summary": "Update a custom channel. The built-in `metalcraft` channel can't be edited.",
        "operationId": "update_channel",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Channel slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateChannelRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Channel"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "gateway"
        ],
        "summary": "Delete a custom channel and its secret. The built-in `metalcraft` channel\ncan't be deleted.",
        "operationId": "delete_channel",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Channel slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/channels/{slug}/events": {
      "get": {
        "tags": [
          "gateway"
        ],
        "summary": "Recent activity for a single channel (by slug), newest first.",
        "operationId": "list_channel_events",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Channel slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/GatewayEvent"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/chats": {
      "get": {
        "tags": [
          "chats"
        ],
        "operationId": "list_chats",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ChatSummary"
                  }
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "chats"
        ],
        "operationId": "post_create_chat",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateChatRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatSummary"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/chats/{id}": {
      "get": {
        "tags": [
          "chats"
        ],
        "operationId": "get_chat",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatDetail"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "chats"
        ],
        "operationId": "delete_chat",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/chats/{id}/compact": {
      "post": {
        "tags": [
          "chats"
        ],
        "summary": "Compact this chat's context now — `/compact`.",
        "description": "Deliberately does everything an automatic compaction does, including handing\nthe summary to memory: it is the most concentrated account of the conversation\nthat will ever exist and the LLM call is already paid for, so a forced\ncompaction that dropped it would quietly be worth less than one that happened\nby itself.\n\nRefuses mid-turn. Compaction rewrites the message list the running turn is\nreading, and \"your context changed under you\" is not a failure mode worth\nhaving.",
        "operationId": "post_chat_compact",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatCompacted"
                }
              }
            }
          },
          "409": {
            "description": "The chat is mid-turn"
          }
        }
      }
    },
    "/api/v1/chats/{id}/context": {
      "get": {
        "tags": [
          "chats"
        ],
        "summary": "What this chat's context costs right now — the read behind `/tokens`, and the\nnumber a client needs to show headroom before someone hits the wall.",
        "operationId": "get_chat_context",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatContext"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/chats/{id}/events": {
      "get": {
        "tags": [
          "chats"
        ],
        "summary": "Live subscription to a chat's *agent-initiated* turns (scheduled follow-ups).\nThe workshop opens this when a chat is on screen so a follow-up that fires\nwhile the user is idle streams in without a page refresh. Normal\nuser-initiated turns still come back on their own `POST .../turn` response.",
        "operationId": "get_chat_events",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "SSE stream of agent-initiated ChatEvent frames",
            "content": {
              "text/event-stream": {
                "schema": {
                  "$ref": "#/components/schemas/ChatEvent"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/chats/{id}/interrupt": {
      "post": {
        "tags": [
          "chats"
        ],
        "summary": "Ask a running turn to stop — the client's stop button.",
        "description": "Returns as soon as the request is recorded, not when the turn ends: the\nexecutor notices at its next step boundary and finishes the turn itself,\nemitting `done{status:\"interrupted\"}` on the chat's event stream. That frame,\nnot this response, is what says the agent has actually stopped.\n\nInterrupting is resumable by construction — the guard stops the executor\nbetween steps, so the partial state is written back like any other turn and\nthe next message continues from it. Nothing is discarded.",
        "operationId": "post_chat_interrupt",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatInterrupt"
                }
              }
            }
          },
          "404": {
            "description": "No such chat"
          }
        }
      }
    },
    "/api/v1/chats/{id}/reset": {
      "post": {
        "tags": [
          "chats"
        ],
        "summary": "End this conversation's context without ending the conversation — `/reset`.",
        "description": "The agent starts its next turn from nothing, and the transcript gains a\ndivider where that happened. Nothing is deleted: this used to be `/clear`,\nwhich dropped the history outright because the transcript and the context were\nthe same list and there was nowhere else for it to live. `DELETE /chats/{id}`\nis still how a conversation actually goes away.\n\nRefuses mid-turn, like compaction: pulling the context out from under a\nrunning turn is not a failure mode worth having.",
        "operationId": "post_chat_reset",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatContext"
                }
              }
            }
          },
          "409": {
            "description": "The chat is mid-turn"
          }
        }
      }
    },
    "/api/v1/chats/{id}/turn": {
      "post": {
        "tags": [
          "chats"
        ],
        "summary": "Run one turn against the chat session. Streams new messages as Server-Sent\nEvents as the agent steps; closes the connection when the executor returns.",
        "operationId": "post_chat_turn",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Chat id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChatTurnRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "SSE stream (text/event-stream) of ChatEvent frames",
            "content": {
              "text/event-stream": {
                "schema": {
                  "$ref": "#/components/schemas/ChatEvent"
                }
              }
            }
          },
          "202": {
            "description": "A turn was already running: the message is queued and will run next. Its frames arrive on `GET /chats/{id}/events`, not on this response.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChatQueued"
                }
              }
            }
          },
          "404": {
            "description": "No such chat"
          }
        }
      }
    },
    "/api/v1/diagnostics": {
      "get": {
        "tags": [
          "diagnostics"
        ],
        "operationId": "list_diagnostics",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/DiagnosticsSessionSummary"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/diagnostics/{id}": {
      "get": {
        "tags": [
          "diagnostics"
        ],
        "operationId": "get_diagnostics_session",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Session id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DiagnosticsSession"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/diagnostics/{id}/trace": {
      "get": {
        "tags": [
          "diagnostics"
        ],
        "summary": "The OTLP trace for one session — where a turn's time actually went.",
        "description": "Deliberately separate from `GET /diagnostics/{id}`: that response already\ncarries every message of every turn, and a trace is read for timings, often\nwhile the session's transcript is already on screen. Bundling them would make\nthe common read pay for the large one.\n\n`404` covers both \"no such session\" and \"that session has no trace\" — a run\nfrom before tracing existed reads the same way to a client, which has nothing\nto show either way.",
        "operationId": "get_diagnostics_trace",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Session id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OTLP/JSON trace document (OpenTelemetry GenAI conventions)"
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/factory-reset": {
      "post": {
        "tags": [
          "admin"
        ],
        "summary": "Erase this pod and restart it as if it had just been provisioned.",
        "description": "Answers **before** it exits, and the report it returns is the last word this\nprocess says — anything the client wants to know about what was removed has\nto come from here, because the next thing to answer on this port will be a\npod with no history of the request.",
        "operationId": "post_factory_reset",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FactoryResetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Wiped; the pod is restarting",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetReport"
                }
              }
            }
          },
          "400": {
            "description": "Confirmation phrase missing or wrong",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Could not read the data directory; nothing was removed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flow-runs": {
      "get": {
        "tags": [
          "flows"
        ],
        "summary": "List persisted flow runs, optionally filtered by `?flow_id=`.",
        "operationId": "list_flow_runs",
        "responses": {
          "200": {
            "description": "Flow run summaries",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/FlowRun"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flow-runs/{run_id}": {
      "get": {
        "tags": [
          "flows"
        ],
        "summary": "Get one flow run by id.",
        "operationId": "get_flow_run",
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "description": "Flow run id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Flow run detail",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowRun"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flow-runs/{run_id}/resume": {
      "post": {
        "tags": [
          "flows"
        ],
        "summary": "Resume a paused flow run.",
        "operationId": "post_resume_flow_run",
        "parameters": [
          {
            "name": "run_id",
            "in": "path",
            "description": "Flow run id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResumeFlowRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Resumed run",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowRunSummary"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flow-templates": {
      "get": {
        "tags": [
          "flows"
        ],
        "operationId": "list_flow_templates",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/FlowTemplateSummary"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flow-templates/{slug}": {
      "get": {
        "tags": [
          "flows"
        ],
        "operationId": "get_flow_template",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Template slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowTemplate"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flows": {
      "get": {
        "tags": [
          "flows"
        ],
        "summary": "`GET /api/v1/flows` — every flow on this pod.",
        "description": "The listing the API never had: until now a client had to already know a flow's\nid to see anything at all, which made \"show me what this pod is set up to do\"\nunanswerable. See `docs/FLOWS_AS_AGENTS_PLAN.md` §3.",
        "operationId": "list_flows",
        "responses": {
          "200": {
            "description": "Every flow installed on this pod",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowList"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flows/install": {
      "post": {
        "tags": [
          "flows"
        ],
        "summary": "Install a registry flow onto this agent: download its `SavedFlow` JSON from\nflows.metalcraftai.com, validate it, and save it into the flows dir (installed\ndisabled). Returns `{ flow, dependencies }` — the dependency report lists any\npacks/personas the flow needs that aren't installed yet.",
        "operationId": "post_install_flow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InstallFlowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Installed flow + dependency report",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstallResult"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "502": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flows/validate": {
      "post": {
        "tags": [
          "flows"
        ],
        "summary": "Check a flow graph without persisting it.",
        "description": "`PUT /flows/{id}` already validates before saving and is still the authority —\nthis exists so an editor can say what is wrong *while someone is typing*,\nwhich is the only time the answer can still change what they do. Same\n`metalcraft_flows::validate` behind both, so the two can never disagree.\n\nNot a 400: an invalid graph is the expected answer to this question, not a bad\nrequest. The status says the check ran; the body says what it found.",
        "operationId": "post_validate_flow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SavedFlow"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowValidation"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flows/{id}": {
      "get": {
        "tags": [
          "flows"
        ],
        "operationId": "get_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The saved flow, graph included",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SavedFlow"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "flows"
        ],
        "operationId": "put_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SavedFlow"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Saved, with the server-stamped `updated_at`",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SavedFlow"
                }
              }
            }
          },
          "400": {
            "description": "The graph is invalid; the body lists why",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "The flow changed since it was loaded — `updated_at` does not match",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "flows"
        ],
        "operationId": "delete_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/flows/{id}/binding": {
      "get": {
        "tags": [
          "flows"
        ],
        "operationId": "get_flow_binding",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowBindingView"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "flows"
        ],
        "operationId": "put_flow_binding",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BindFlowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FlowBindingView"
                }
              }
            }
          },
          "400": {
            "description": "Flow names personas outside the roster",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flows/{id}/check-dependencies": {
      "post": {
        "tags": [
          "flows"
        ],
        "summary": "Report whether this pod satisfies the integrations an already-installed flow\ndeclares in its `requires` block. One outcome per pack.",
        "description": "It reports rather than installs. Fetching a pack from here was a second install\npath into a second layout; an integration reaches a pod inside an agent pack the\noperator installs, through `POST /api/v1/agent-packs/install` and nowhere else.",
        "operationId": "post_check_flow_dependencies",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Per-pack outcomes",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstallDependenciesResponse"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/flows/{id}/run": {
      "post": {
        "tags": [
          "flows"
        ],
        "operationId": "post_run_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RunFlowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "v2 flows return a FlowRunSummary; v1 flows return a RunFlowResponse",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunFlowOutput"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/gateway/activity": {
      "get": {
        "tags": [
          "gateway"
        ],
        "summary": "Recent gateway activity across all channels, including inbound messages that\nmatched no channel (the global Network view). Newest first.",
        "operationId": "list_gateway_activity",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/GatewayEvent"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/gateway/metalcraft/connect": {
      "post": {
        "tags": [
          "gateway"
        ],
        "summary": "Connect: fetch config + wire the webhook + enable the channel. 409 until verified.",
        "operationId": "gateway_metalcraft_connect",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MgConnectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Connected"
          }
        }
      }
    },
    "/api/v1/gateway/metalcraft/disconnect": {
      "post": {
        "tags": [
          "gateway"
        ],
        "summary": "Disconnect: disable the metalcraft-gateway channel + drop its secrets. Idempotent.",
        "operationId": "gateway_metalcraft_disconnect",
        "responses": {
          "200": {
            "description": "Disconnected"
          }
        }
      }
    },
    "/api/v1/gateway/metalcraft/register": {
      "post": {
        "tags": [
          "gateway"
        ],
        "summary": "Inline register: proxy to the gateway with the pod's token; returns `verify_code`.",
        "operationId": "gateway_metalcraft_register",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MgRegisterRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Registered"
          }
        }
      }
    },
    "/api/v1/gateway/metalcraft/status": {
      "get": {
        "tags": [
          "gateway"
        ],
        "summary": "Registration/verification/connection state for the workshop's Connect panel.",
        "operationId": "gateway_metalcraft_status",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GatewayStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/gateway/metalcraft/unregister": {
      "post": {
        "tags": [
          "gateway"
        ],
        "summary": "Unregister: give the number back at the gateway, then disconnect locally.",
        "description": "The exit that `disconnect` is not. Disconnecting stops this pod receiving and\nleaves the account's registration standing — number still bound and verified,\ndedicated number still out of the pool, managed integration still routing to\na consumer that left. This is the one that ends all of that, and until it\nexisted only a client holding the account PAT could do it, which excludes\nevery client that reaches the account through its pod.",
        "operationId": "gateway_metalcraft_unregister",
        "responses": {
          "200": {
            "description": "Unregistered"
          },
          "502": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/inference": {
      "get": {
        "tags": [
          "keys"
        ],
        "operationId": "get_inference_status",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InferenceStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/info": {
      "get": {
        "tags": [
          "agent"
        ],
        "summary": "Agent identity/version + config the Workshop reads. `version` drives the\nSettings tab's \"which build is live\" check; `default_persona` is the persona\nthe Workshop's New Chat modal defaults to (set `METALCRAFT_DEFAULT_PERSONA`\nto override; falls back to the orchestrator, which delegates to specialists).",
        "operationId": "agent_info",
        "responses": {
          "200": {
            "description": "Agent name, version, and default persona"
          }
        }
      }
    },
    "/api/v1/integrations": {
      "get": {
        "tags": [
          "integrations"
        ],
        "operationId": "list_integrations",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/IntegrationSummary"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/integrations/{id}": {
      "get": {
        "tags": [
          "integrations"
        ],
        "operationId": "get_integration",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Pack id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IntegrationDetail"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/integrations/{id}/enabled": {
      "put": {
        "tags": [
          "integrations"
        ],
        "summary": "Retired. An integration is no longer independently enabled or disabled —\nan agent pack is the install unit, and the packs it vendors are simply present\n(see `docs/AGENT_PACKS_PLAN.md`).",
        "description": "This answers 410 rather than quietly succeeding: a toggle that returns 204 and\nchanges nothing is worse than one that says it is gone, because the UI would go\non showing a state the runtime does not honour.",
        "operationId": "put_integration_enabled",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Pack id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetEnabledRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "410": {
            "description": "Retired — uninstall the agent pack instead",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/keys": {
      "get": {
        "tags": [
          "keys"
        ],
        "operationId": "list_keys",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/KeyEntry"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/keys/recommended": {
      "get": {
        "tags": [
          "keys"
        ],
        "summary": "Keys recommended by enabled packs, each flagged configured/missing. Lets the\nkey store UI surface \"these enabled packs need these keys\" without the user\nhaving to read each pack's manifest.",
        "operationId": "list_recommended_keys",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/RecommendedKey"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/keys/{name}": {
      "put": {
        "tags": [
          "keys"
        ],
        "summary": "Upsert a key. The name comes from the path; the raw value (and an optional\ntarget `channel_id`) from the body. Managed keys — a platform-injected global\n(env-authoritative) or a provisioner-backed channel's secrets — are read-only\nand rejected here.",
        "operationId": "put_key",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Key name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/KeyValueBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      },
      "delete": {
        "tags": [
          "keys"
        ],
        "summary": "Delete a key. An optional `?channel_id=` targets a channel's secret scope;\nomitted means the global scope. Managed keys are read-only and rejected.",
        "operationId": "delete_key",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Key name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "channel_id",
            "in": "query",
            "description": "Channel scope for a channel secret",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/keys/{name}/reveal": {
      "get": {
        "tags": [
          "keys"
        ],
        "summary": "Reveal a stored key's **raw** value. An optional `?channel_id=` targets a\nchannel's secret scope; omitted means global. This is the one endpoint that\nreturns an unmasked value — an explicit, user-initiated action in the Keys UI.\nOnly returns values physically present in the store (a derived/env-only value\nlike a provisioner's `API_KEY` is not stored, so there's nothing to reveal).",
        "operationId": "reveal_key",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Key name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "channel_id",
            "in": "query",
            "description": "Channel scope for a channel secret",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/KeyRevealResponse"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/lockfile": {
      "get": {
        "tags": [
          "lockfile"
        ],
        "summary": "The pod's `metalcraft.lock` — every registry pack/flow pinned to an exact\nversion + content hash. Export it to clone a pod's toolset, or feed it to\n`/lockfile/restore` on a rebuilt pod.",
        "operationId": "get_lockfile",
        "responses": {
          "200": {
            "description": "The install lockfile",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Lock"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/lockfile/restore": {
      "post": {
        "tags": [
          "lockfile"
        ],
        "summary": "Reinstall everything in the lockfile at its pinned version, verifying each against\nits locked content hash — the reproducible-rebuild path for a fresh or cloned pod.\nReturns one outcome per entry; a failure on one entry doesn't stop the rest.",
        "operationId": "post_lockfile_restore",
        "responses": {
          "200": {
            "description": "Per-entry restore outcomes",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestoreResult"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics": {
      "get": {
        "tags": [
          "diagnostics"
        ],
        "summary": "What this process is holding, and the ceilings bounding it.",
        "description": "Authenticated like the rest of `/api/v1` — it names conversations only by\ncount, but a size profile of a pod's traffic is still the pod's business.",
        "operationId": "get_metrics",
        "responses": {
          "200": {
            "description": "Resident memory and the limits bounding it",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryMetrics"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/personas": {
      "get": {
        "tags": [
          "personas"
        ],
        "summary": "Every persona on this pod: the pod's own, plus those an enabled pack vendors.",
        "description": "Locals shadow packs on a slug collision, which is the same precedence the\nrunner resolves with — so what is listed here is what would actually run.\n\nSummaries only. A persona's system prompt is most of its bytes and none of an\nindex's job; `GET /personas/{slug}` is the whole document.",
        "operationId": "list_personas",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/PersonaSummary"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/personas/{slug}": {
      "get": {
        "tags": [
          "personas"
        ],
        "operationId": "get_persona",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Persona slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Persona"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "personas"
        ],
        "operationId": "put_persona",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Persona slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Persona"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      },
      "delete": {
        "tags": [
          "personas"
        ],
        "operationId": "delete_persona",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Persona slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/projects": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "`GET /api/v1/projects` — what this pod is working towards on its own.",
        "operationId": "list_projects",
        "responses": {
          "200": {
            "description": "Goals on this pod",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectList"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "projects"
        ],
        "summary": "`POST /api/v1/projects` — set a project.",
        "description": "The consent point, and the only one: this creates the project **and** the agent\nthat will pursue it, because \"work at this while I am not here\" is one\ndecision rather than two.",
        "operationId": "post_project",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateProjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectRow"
                }
              }
            }
          },
          "400": {
            "description": "Empty project, or no agent preset to run it as",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Too many projects already ticking",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/projects/{id}": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "`GET /api/v1/projects/{id}` — one project, with the scratchpad a client renders.",
        "operationId": "get_project",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectDetail"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "projects"
        ],
        "summary": "`DELETE /api/v1/projects/{id}` — forget a project.",
        "description": "Its agent survives: instances are never deleted on a timer, and what this one\nlearned about the repo outlives the errand it learned it on.",
        "operationId": "delete_project",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted"
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "patch": {
        "tags": [
          "projects"
        ],
        "summary": "`PATCH /api/v1/projects/{id}` — pause, resume, retarget, or answer.",
        "description": "Answering is the important one: a blocked project is stopped until a person\nsays something, and this is where that lands. The answer goes into the\nscratchpad rather than into a message, because the tick that acts on it will\nhave read nothing else.",
        "operationId": "patch_project",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateProjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectRow"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Resuming would exceed the active ceiling",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/projects/{id}/findings": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "`GET /api/v1/projects/{id}/findings` — what an audit project has turned up.",
        "description": "Empty for a build project, which keeps no ledger: its findings are its plan.",
        "operationId": "get_project_findings",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectFindings"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/projects/{id}/journal": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "`GET /api/v1/projects/{id}/journal` — what it has been doing, one line per tick.",
        "operationId": "get_project_journal",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "How many of the most recent entries. Defaults to 50.",
            "required": false,
            "schema": {
              "type": [
                "integer",
                "null"
              ],
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectJournal"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/projects/{id}/scratchpad": {
      "put": {
        "tags": [
          "projects"
        ],
        "summary": "`PUT /api/v1/projects/{id}/scratchpad` — fix the project's memory by hand.",
        "description": "The repair hatch. A groom that went wrong, or a plan that has drifted, is\notherwise only correctable by asking the agent nicely and hoping. The\nprevious version is snapshotted like any other write, so this cannot be the\nlast copy either.",
        "operationId": "put_project_scratchpad",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WriteScratchpadRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectDetail"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/projects/{id}/tasks": {
      "get": {
        "tags": [
          "projects"
        ],
        "summary": "`GET /api/v1/projects/{id}/tasks` — the plan, as records.",
        "operationId": "get_project_tasks",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ProjectTask"
                  }
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "projects"
        ],
        "summary": "`PUT /api/v1/projects/{id}/tasks` — steer a running project.",
        "description": "The point of a plan being records rather than prose: a person watching a project\ncan add a task, re-order the list, or drop something that has become\npointless, without editing markdown and hoping the next tick honours it.\n\nDeliberately a whole-list replace, like the scratchpad write next to it: the\nclient edits what it just read, and there is never a question of which write\nwon. There is one other writer — the tick — and a person and a tick racing on\nthe same list is the same race the scratchpad already has, resolved the same\nway.",
        "operationId": "put_project_tasks",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WriteTasksRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ProjectTask"
                  }
                }
              }
            }
          },
          "400": {
            "description": "The list is not a usable plan",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/projects/{id}/tick": {
      "post": {
        "tags": [
          "projects"
        ],
        "summary": "`POST /api/v1/projects/{id}/tick` — run now, rather than at the next heartbeat.",
        "description": "One of the three levers a person gets over a project, and the reason the\nother two are usable: change the goal or the period and this is how you see\nthe effect without waiting fifteen minutes for it.\n\nA **request**, not a preemption. It raises a flag the dispatcher honours on\nits next pass; if a tick is already running, the flag survives it and fires\nwhen it ends. Two turns of the same worker at once is the one thing a project\nmust never do, so this will not start one.\n\nA paused project stays paused: \"run now\" is about *when*, not about\noverriding a decision somebody already took.",
        "operationId": "post_project_tick",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Project id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "Accepted; the dispatcher will pick it up",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectRow"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "The project is not in a state that ticks",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/scheduled-flows": {
      "get": {
        "tags": [
          "flows"
        ],
        "summary": "`GET /api/v1/scheduled-flows` — everything this pod will do on its own.",
        "description": "The complete answer: nothing else fires a flow on a timer. An empty list means\nthis pod does nothing unless somebody asks it to.",
        "operationId": "list_scheduled_flows",
        "parameters": [
          {
            "name": "flow_id",
            "in": "query",
            "description": "Only schedules of this flow.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          {
            "name": "instance_id",
            "in": "query",
            "description": "Only schedules armed to this agent.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Scheduled flows on this pod",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduledFlowList"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "flows"
        ],
        "summary": "`POST /api/v1/scheduled-flows` — arm a flow.",
        "description": "This is the consent point: it creates the schedule **and** the agent that will\nrun it, since \"run this while nobody is watching\" is one decision rather than\ntwo. Schedules of one flow share an agent unless `instance_id` says\notherwise, so the evening run remembers the morning one.",
        "operationId": "post_scheduled_flow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateScheduledFlowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Armed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduledFlowRow"
                }
              }
            }
          },
          "400": {
            "description": "Bad trigger, or a persona outside the flow's roster",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "No such flow",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/scheduled-flows/preview": {
      "post": {
        "tags": [
          "flows"
        ],
        "summary": "`POST /api/v1/scheduled-flows/preview` — when *would* this fire?",
        "description": "Takes an unsaved spec so an editor can answer the question before committing to\nit. An empty `next_runs` on a cron trigger means this pod cannot parse it — the\nthing a person most needs to know before saving.",
        "operationId": "post_schedule_preview",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PreviewScheduleRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SchedulePreview"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/scheduled-flows/{id}": {
      "get": {
        "tags": [
          "flows"
        ],
        "operationId": "get_scheduled_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Scheduled flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduledFlowRow"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "flows"
        ],
        "operationId": "put_scheduled_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Scheduled flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateScheduledFlowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduledFlowRow"
                }
              }
            }
          },
          "400": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "delete": {
        "tags": [
          "flows"
        ],
        "summary": "`DELETE /api/v1/scheduled-flows/{id}` — disarm.",
        "description": "**The agent and everything it remembers are kept.** Disarming is \"stop running\nthis on a timer\", not \"destroy the thing that was running it\", and a client\nshould not imply otherwise.",
        "operationId": "delete_scheduled_flow",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Scheduled flow id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Disarmed; the agent and its memory are kept"
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/scheduled-tasks": {
      "get": {
        "tags": [
          "scheduled-tasks"
        ],
        "summary": "List scheduled follow-ups (pending + recently completed), newest first.",
        "operationId": "list_scheduled_tasks",
        "responses": {
          "200": {
            "description": "Scheduled follow-ups",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/scheduled-tasks/{id}": {
      "delete": {
        "tags": [
          "scheduled-tasks"
        ],
        "summary": "Cancel a pending scheduled follow-up.",
        "operationId": "delete_scheduled_task",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Task id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/services/octaweave": {
      "get": {
        "tags": [
          "agent-packs"
        ],
        "summary": "What this pod is to Octaweave, and whether the tools are installed.",
        "description": "A read, not a write: there is nothing to configure. The pod presents the Metalcraft\ntoken it already holds, and the only human step is linking the two accounts once —\nwhich happens on Octaweave's own page, at `link_url`.",
        "operationId": "get_octaweave_status",
        "responses": {
          "200": {
            "description": "This pod's standing with Octaweave",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OctaweaveConnection"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/settings": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "What this pod is set to prefer.",
        "operationId": "get_pod_settings",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PodSettings"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "settings"
        ],
        "summary": "Replace them. The whole document, so clearing a preference is expressible.",
        "operationId": "put_pod_settings",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PodSettings"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PodSettings"
                }
              }
            }
          },
          "400": {
            "description": "A preference this pod cannot honour, e.g. an unknown timezone",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/skills": {
      "get": {
        "tags": [
          "skills"
        ],
        "summary": "Every skill on this pod: the pod's own, plus those an enabled pack vendors.",
        "description": "Summaries only — a skill *is* its markdown body, so an index that carried it\nwould be the whole skills directory in one response. `GET /skills/{slug}` has\nthe body.",
        "operationId": "list_skills",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/SkillSummary"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/skills/{slug}": {
      "get": {
        "tags": [
          "skills"
        ],
        "operationId": "get_skill",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Skill slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Skill"
                }
              }
            }
          },
          "404": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": [
          "skills"
        ],
        "operationId": "put_skill",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Skill slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Skill"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Saved"
          }
        }
      },
      "delete": {
        "tags": [
          "skills"
        ],
        "operationId": "delete_skill",
        "parameters": [
          {
            "name": "slug",
            "in": "path",
            "description": "Skill slug",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deleted"
          }
        }
      }
    },
    "/api/v1/snapshot": {
      "get": {
        "tags": [
          "agent"
        ],
        "operationId": "get_snapshot",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PodSnapshot"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/timezones": {
      "get": {
        "tags": [
          "settings"
        ],
        "summary": "Every timezone this pod can resolve, grouped by region — the list a picker\nshould offer.",
        "description": "Published rather than left to each client's own platform list, because the\ntwo drift: a zone the phone knows and this build of the tz database does not\nis a save that fails *after* somebody chose it.",
        "operationId": "list_timezones",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TimezoneRegion"
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "AgentInstance": {
        "type": "object",
        "required": [
          "id",
          "agent_preset",
          "name",
          "persona",
          "created_at"
        ],
        "properties": {
          "agent_pack": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent pack that provided the preset, when it came from one."
          },
          "agent_preset": {
            "type": "string",
            "description": "The agent preset this instance was created from. **Immutable for its life** —\nits memory is seeded from that preset, so swapping it mid-life is incoherent.\nSwitching agents means starting a new instance."
          },
          "created_at": {
            "type": "string"
          },
          "created_from_version": {
            "type": [
              "string",
              "null"
            ],
            "description": "Diagnostics only. Personas and skills **follow** the installed pack version\n(see the plan's §5.4); this records what it was born against."
          },
          "id": {
            "type": "string"
          },
          "last_active_at": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "origin": {
            "$ref": "#/components/schemas/InstanceOrigin"
          },
          "orphaned_from": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when the agent pack that provided this agent's preset withdrew it — on\nupdate or on a forced uninstall. The agent keeps its memory and its\nconversations and goes on working against a frozen copy of the preset; this\nrecords what happened, so a UI can say so rather than presenting a\npack-provided agent that is quietly no longer pack-provided."
          },
          "persona": {
            "type": "string",
            "description": "The instance's current persona — starts at the preset's default and moves\nwithin its roster."
          },
          "persona_fallback_from": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when an update withdrew the persona this agent was using and it fell back\nto the preset's default. Reported rather than silent: the agent's voice\nchanged, and nobody asked for that."
          }
        }
      },
      "AgentPackManifest": {
        "type": "object",
        "required": [
          "id",
          "name",
          "version"
        ],
        "properties": {
          "author": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Author"
              }
            ]
          },
          "category": {
            "type": [
              "string",
              "null"
            ]
          },
          "content_sha256": {
            "type": [
              "string",
              "null"
            ],
            "description": "Hash of every file in the archive **except `agent_pack.json`**, so the\nmanifest can carry the hash of what it describes without hashing itself."
          },
          "description": {
            "type": "string"
          },
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "handle": {
            "type": [
              "string",
              "null"
            ],
            "description": "The registry handle this pack is published under (`amy_kitchen`)."
          },
          "id": {
            "type": "string"
          },
          "license": {
            "type": [
              "string",
              "null"
            ]
          },
          "manifest_version": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "name": {
            "type": "string"
          },
          "parent": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Parent",
                "description": "Fork lineage."
              }
            ]
          },
          "presets": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The agent presets this pack provides. Exactly one today — the registry\nenforces it — but an array so multi-preset \"crews\" stay additive later."
          },
          "provides": {
            "$ref": "#/components/schemas/Provides"
          },
          "requires_env": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EnvRequirement"
            },
            "description": "Derived at build time and **re-derived at install**; a manifest that\ndisagrees with its own contents is rejected rather than trusted."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "version": {
            "type": "string"
          }
        }
      },
      "AgentPackPreview": {
        "type": "object",
        "description": "What installing this pack would grant, and what it would change.\n\nThe install dialog's whole reason to exist. Without this a client could only\nshow a permission summary *after* installing, which is not consent — or parse the\narchive itself, duplicating the validator that has to be authoritative anyway.\n\nEverything here is derived from the archive's own bytes. The consent summary\nnever comes from what the author wrote about their pack.",
        "required": [
          "manifest",
          "consent",
          "content_sha256",
          "source",
          "missing_env",
          "preset_collisions"
        ],
        "properties": {
          "consent": {
            "$ref": "#/components/schemas/ConsentSummary"
          },
          "content_sha256": {
            "type": "string",
            "description": "Content hash of the archive as received, so a UI can show what it is about to\ninstall and compare it against what a registry advertised."
          },
          "installed_version": {
            "type": [
              "string",
              "null"
            ],
            "description": "The version already installed under this id, if any. Present means this is an\nupgrade (or a downgrade), not a first install, and the dialog should say so."
          },
          "manifest": {
            "$ref": "#/components/schemas/AgentPackManifest"
          },
          "missing_env": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Credentials the pod does not have yet. A warning, not a blocker: the pack\ninstalls and its tools fail clearly at call time until `key_set` fixes it."
          },
          "preset": {
            "type": [
              "string",
              "null"
            ],
            "description": "The single preset this pack provides."
          },
          "preset_collisions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Preset slugs another installed pack already provides."
          },
          "source": {
            "type": "string",
            "description": "Where the bytes came from — a URL, a path, or `\"upload\"`."
          }
        }
      },
      "AgentPreset": {
        "type": "object",
        "required": [
          "slug",
          "name",
          "default_persona"
        ],
        "properties": {
          "avatar": {
            "type": [
              "string",
              "null"
            ]
          },
          "default_persona": {
            "type": "string"
          },
          "delegates_to_any_persona": {
            "type": "boolean",
            "description": "**Unrestricted delegation.** Normally a preset's `sub_agent` calls are\nconfined to [`Self::callable_personas`] — the roster it declared. An\norchestrator-style preset is the exception: its whole job is to hand work to\nwhichever specialist fits, including personas that arrived later with an agent\npack it has never heard of. With this set, every persona installed on the pod\njoins its delegation roster (its own still listed first).\n\nIt is a real widening of what the agent can reach, so it stays opt-in per\npreset and is logged when an agent pack ships a preset that claims it."
          },
          "description": {
            "type": "string"
          },
          "integrations": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Reads `integration_packs` too — the pre-0.30 name, still present in every\npreset authored before the rename."
          },
          "library": {
            "type": "boolean",
            "description": "**A library, not an agent.** The preset exists to carry a persona roster,\nskills and integrations into the pod; nothing about it is startable. No\ninstance may be minted from it, it is not offered in a picker, and no flow\nmay be bound to it.\n\nThis is what an agent pack sets when its value is the specialist it ships\nrather than a dedicated agent to talk to. Nothing is lost by it: the\npersonas and skills install exactly as before, and `general-agent`\n([`Self::delegates_to_any_persona`]) reaches every persona on the pod — so\nthe orchestrator can still hand work to the specialist. What goes away is\nthe \"start a new agent as this\" entry that nobody wanted to pick.\n\nEnforced at the doors that mint an [`AgentInstance`] rather than in\n[`Self::load`], because resolving a persona or skill through this preset is\nprecisely what must keep working.\n\n[`AgentInstance`]: crate::agent_instance::AgentInstance"
          },
          "manifest_version": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "memories": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/MemoriesRef"
              }
            ]
          },
          "model": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ModelFloor"
              }
            ]
          },
          "name": {
            "type": "string"
          },
          "personas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PresetPersona"
            }
          },
          "requires_env": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "slug": {
            "type": "string"
          },
          "tagline": {
            "type": [
              "string",
              "null"
            ]
          },
          "version": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "ApiToolSummary": {
        "type": "object",
        "required": [
          "name",
          "description"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "read_only": {
            "type": "boolean"
          }
        }
      },
      "ArmConsent": {
        "type": "object",
        "description": "The resolved content of the arm dialog.",
        "required": [
          "preset_name",
          "domains",
          "requires_env",
          "missing_env",
          "mutating_tools",
          "tool_count",
          "base_memories"
        ],
        "properties": {
          "base_memories": {
            "type": "integer",
            "description": "Seed memories the agent starts from. It accumulates more on every run.",
            "minimum": 0
          },
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Origins its tools can reach."
          },
          "missing_env": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Credentials the pod does not have — those tools will fail at 3am rather than\nat a moment anyone is looking."
          },
          "mutating_tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tools that can change something on the other end."
          },
          "preset_name": {
            "type": "string",
            "description": "Display name of the preset this flow runs as."
          },
          "requires_env": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Credentials it will use."
          },
          "tool_count": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "ArmedSchedule": {
        "type": "object",
        "required": [
          "schedule_id",
          "name",
          "instance_id"
        ],
        "properties": {
          "instance_id": {
            "type": "string"
          },
          "instance_name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Absent if the instance was deleted out from under it."
          },
          "name": {
            "type": "string",
            "description": "Its label, so the arm dialog can say *which* schedule without showing an\nopaque id."
          },
          "schedule_id": {
            "type": "string",
            "description": "The scheduled flow's id."
          }
        }
      },
      "Author": {
        "type": "object",
        "required": [
          "handle"
        ],
        "properties": {
          "display_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "handle": {
            "type": "string"
          },
          "sub": {
            "type": [
              "string",
              "null"
            ],
            "description": "The author's Metalcraft ID subject, when the registry recorded one."
          }
        }
      },
      "BindFlowRequest": {
        "type": "object",
        "properties": {
          "preset": {
            "type": [
              "string",
              "null"
            ],
            "description": "The preset slug. `null` clears the binding back to the default agent."
          }
        }
      },
      "BroadcasterMemory": {
        "type": "object",
        "description": "Live event buses (`GET /chats/{id}/events`).",
        "required": [
          "count",
          "subscribed"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "description": "Buses in the registry.",
            "minimum": 0
          },
          "subscribed": {
            "type": "integer",
            "description": "Of those, how many have a subscriber. The gap between the two is what the\nsweeper reclaims; a gap that keeps growing means a cleanup path is being\nmissed.",
            "minimum": 0
          }
        }
      },
      "Channel": {
        "type": "object",
        "description": "A channel as surfaced to callers/UI: a named connection. `managed` marks the\nbuilt-in `metalcraft` channel (secret is the pod token; not user-editable).\nThe secret value itself is never included here.",
        "required": [
          "slug",
          "name",
          "url"
        ],
        "properties": {
          "active_number": {
            "type": [
              "string",
              "null"
            ],
            "description": "The gateway number bound to this channel, if any."
          },
          "agent_preset": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent that answers inbound on this channel. Its instance — and everything\nthat instance remembers — is what gives the channel continuity across idle\nresets. Absent means the pod default.\n\nWithout this a channel could only ever be the default agent: installing an\nagent pack and pointing an SMS number at it was not expressible, and the\nchannel's memory base was one that had never been built."
          },
          "connected": {
            "type": "boolean",
            "description": "Whether the channel has completed a gateway connect (link fields present)."
          },
          "enabled": {
            "type": "boolean"
          },
          "integration_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The gateway integration UUID this channel is bound to. Inbound messages\ncarrying this `source_id` route here; also the send-time sender selector."
          },
          "managed": {
            "type": "boolean",
            "description": "True only for the built-in `metalcraft` channel."
          },
          "model": {
            "type": [
              "string",
              "null"
            ],
            "description": "Model override for inbound runs on this channel."
          },
          "name": {
            "type": "string"
          },
          "persona": {
            "type": [
              "string",
              "null"
            ],
            "description": "Persona that answers inbound on this channel (default: the agent's own)."
          },
          "slug": {
            "type": "string"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "ChatCompacted": {
        "type": "object",
        "description": "The result of a forced compaction.",
        "required": [
          "compacted",
          "tokens_before",
          "tokens_after",
          "messages_before",
          "messages_after"
        ],
        "properties": {
          "compacted": {
            "type": "boolean",
            "description": "False when there was nothing old enough to summarize — not an error, and\nthe honest answer for a short conversation."
          },
          "messages_after": {
            "type": "integer",
            "minimum": 0
          },
          "messages_before": {
            "type": "integer",
            "minimum": 0
          },
          "summary": {
            "type": [
              "string",
              "null"
            ],
            "description": "The summary that replaced the old half, when one was produced."
          },
          "tokens_after": {
            "type": "integer",
            "minimum": 0
          },
          "tokens_before": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "ChatContext": {
        "type": "object",
        "description": "What a chat's context currently costs.",
        "required": [
          "estimated_tokens",
          "message_count",
          "context_window",
          "compact_threshold_tokens",
          "would_compact"
        ],
        "properties": {
          "compact_threshold_tokens": {
            "type": "integer",
            "description": "Automatic compaction fires above this. A client can render the headroom.",
            "minimum": 0
          },
          "context_window": {
            "type": "integer",
            "description": "The window compaction sizes against.",
            "minimum": 0
          },
          "estimated_tokens": {
            "type": "integer",
            "description": "Rough estimate (~4 chars per token), the same one compaction decides on.\nNot the provider's count — good enough to answer \"how full is this?\".",
            "minimum": 0
          },
          "message_count": {
            "type": "integer",
            "minimum": 0
          },
          "would_compact": {
            "type": "boolean",
            "description": "Whether the next turn would compact on its own."
          }
        }
      },
      "ChatDetail": {
        "type": "object",
        "required": [
          "id",
          "persona_slug",
          "model_name",
          "created_at",
          "messages"
        ],
        "properties": {
          "busy": {
            "type": "boolean",
            "description": "Whether a turn is running right now.\n\nAnswers the question a reopened chat cannot answer from its messages: the\nlast thing said is the person's own, and whether the agent is working on\nit or has been idle for an hour is the difference between a composer that\noffers Stop and one that offers Send. Without it a client that opened a\nchat mid-turn drew a finished conversation and let someone type into it."
          },
          "created_at": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "messages": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ChatMessageWire"
            }
          },
          "model_name": {
            "type": "string"
          },
          "persona_slug": {
            "type": "string"
          },
          "plan": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanStep"
            },
            "description": "The plan of the turn running right now, empty when none is. Lets a client\nthat arrives mid-turn render the plan it missed the frames for."
          }
        }
      },
      "ChatEvent": {
        "oneOf": [
          {
            "type": "object",
            "description": "Marks the start of a turn — emitted once at the top of `post_chat_turn`\nso the workshop can open a new group in the transcript.",
            "required": [
              "turn_index",
              "user_message",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "turn_started"
                ]
              },
              "session_id": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Diagnostics session directory name for this chat, so the workshop can\ndeep-link a turn (or a turn error) to its session logs. `None` for a\nchat with no active logger — e.g. one reloaded after an agent restart."
              },
              "turn_index": {
                "type": "integer",
                "minimum": 0
              },
              "user_message": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "LLM call has started. Paired with a later `LlmCompleted`.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "llm_started"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "LLM call finished. `messages` is any new assistant message(s); tool\ncalls produced by this LLM step are emitted as separate `ToolStarted`\nevents immediately after.",
            "required": [
              "messages",
              "duration_ms",
              "kind"
            ],
            "properties": {
              "duration_ms": {
                "type": "integer",
                "format": "int64",
                "minimum": 0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "llm_completed"
                ]
              },
              "messages": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ChatMessageWire"
                }
              }
            }
          },
          {
            "type": "object",
            "description": "A tool the LLM requested is about to run.",
            "required": [
              "tool_call_id",
              "name",
              "args",
              "kind"
            ],
            "properties": {
              "args": {},
              "kind": {
                "type": "string",
                "enum": [
                  "tool_started"
                ]
              },
              "name": {
                "type": "string"
              },
              "tool_call_id": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "The tool finished. `result` is the `ToolResult` message that was\nappended to state.",
            "required": [
              "tool_call_id",
              "name",
              "duration_ms",
              "result",
              "kind"
            ],
            "properties": {
              "duration_ms": {
                "type": "integer",
                "format": "int64",
                "minimum": 0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "tool_completed"
                ]
              },
              "name": {
                "type": "string"
              },
              "result": {
                "$ref": "#/components/schemas/ChatMessageWire"
              },
              "tool_call_id": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "The agent's user-facing message, produced by a `say_to_user` or\n`ask_user` tool call. In tool-only mode this — not free-text\n`LlmCompleted` content — is the assistant's message; the workshop renders\nit as the reply bubble. The underlying tool start/finish events are\nsuppressed so the reply isn't also shown as a raw tool card.\n\n`awaiting_reply` distinguishes the two ways a turn ends: an answer closes\nthe exchange, a question leaves it open and the client should invite a\nresponse (and may render `options` as tappable choices). Both are omitted\nwhen unset, so a client written before `ask_user` existed still sees a\nperfectly ordinary reply — which is the correct degradation, since the\nquestion is in the text either way.",
            "required": [
              "content",
              "kind"
            ],
            "properties": {
              "awaiting_reply": {
                "type": "boolean"
              },
              "content": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "reply"
                ]
              },
              "options": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          },
          {
            "type": "object",
            "description": "What the turn is doing *before* the model is called — work that emits no\nother frame and that a client would otherwise render as an\nundifferentiated wait.\n\nCompaction is a whole extra summarization LLM call and recall is an\nembeddings call; both run ahead of the executor, so nothing between\n`turn_started` and the first `llm_started` used to say a word. `phase` is\nan open string (`crate::runtime::phase`) precisely so a client older than\na phase can drop the frame instead of breaking on it.",
            "required": [
              "phase",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "phase"
                ]
              },
              "phase": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "A message was accepted while a turn was already running, and is waiting\nits turn rather than starting one.\n\nEmitted on the chat's live bus rather than on the sending request, which\nhas already been answered with 202 — the point of queueing is that the\nsender does not hold a connection open waiting. `position` is 1 for the\nnext message to run.",
            "required": [
              "message",
              "position",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "queued"
                ]
              },
              "message": {
                "type": "string"
              },
              "position": {
                "type": "integer",
                "minimum": 0
              }
            }
          },
          {
            "type": "object",
            "description": "This turn's plan, as `update_plan` wrote it. Sent on every change,\nincluding the empty list a new turn starts with, so a client renders the\nplan as it stands rather than accumulating every version of it.",
            "required": [
              "steps",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "plan"
                ]
              },
              "steps": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/PlanStep"
                }
              }
            }
          },
          {
            "type": "object",
            "description": "A queued message joined the turn that was already running, rather than\nwaiting for it to end. The client should stop showing it as pending.",
            "required": [
              "message",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "injected"
                ]
              },
              "message": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "Terminal event. `status` is \"completed\" | \"interrupted\" | \"failed\".",
            "required": [
              "status",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "done"
                ]
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "status": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "A classified turn failure, safe to show the user. Emitted instead of a\nbare `Done{status:\"failed\"}` so frontends can render a friendly message\n(and branch on `code`) rather than the raw provider-error chain. `code`\nis the machine-readable identifier (see `runtime::ErrorCode`); `message`\nis the user-facing text; `retryable` hints whether \"try again\" is useful.\nA `done` still follows so lifecycle handling is unchanged for old clients.",
            "required": [
              "code",
              "message",
              "retryable",
              "kind"
            ],
            "properties": {
              "code": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "error"
                ]
              },
              "message": {
                "type": "string"
              },
              "retryable": {
                "type": "boolean"
              }
            }
          },
          {
            "type": "object",
            "description": "The conversation's context ended — see [`ChatMessageWire::Reset`].\n\nNot part of a turn's lifecycle: it arrives on its own, usually with nobody\nhaving typed anything. Without it a client watching a flow's agent would\nsee the 3am firing start from nothing and have no way to say why, since\nthe divider it should draw is only in the transcript it loaded hours ago.",
            "required": [
              "at",
              "reason",
              "kind"
            ],
            "properties": {
              "at": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "reset"
                ]
              },
              "reason": {
                "type": "string"
              }
            }
          }
        ],
        "description": "SSE event wire format. One JSON object per event. The `kind` field\ndiscriminates; payloads vary by kind. Events form a lifecycle:\n  `turn_started` → `phase`* → (`llm_started` → `llm_completed`\n                  → `tool_started`* → `tool_completed`*)+\n                  → `done`\n(`tool_started` and `tool_completed` can repeat per LLM step; `phase` frames\ncover the pre-model work that would otherwise be silent.)"
      },
      "ChatInterrupt": {
        "type": "object",
        "required": [
          "stopping"
        ],
        "properties": {
          "stopping": {
            "type": "boolean",
            "description": "True when a turn was running and has been asked to stop. False when the\nchat was already idle — the turn finished on its own between the press\nand this request, which is not a failure and must not be shown as one."
          }
        }
      },
      "ChatMemory": {
        "type": "object",
        "description": "The resident cost of one part of the process.",
        "required": [
          "resident",
          "unmeasured",
          "turns_in_flight",
          "context_bytes",
          "archived_bytes",
          "in_flight_bytes",
          "messages"
        ],
        "properties": {
          "archived_bytes": {
            "type": "integer",
            "format": "int64",
            "description": "Bytes of closed-off history kept beside the context. Append-only today:\na reset moves messages here and nothing ever moves them out, which is the\nresidency this endpoint exists to make visible.",
            "minimum": 0
          },
          "context_bytes": {
            "type": "integer",
            "format": "int64",
            "description": "Bytes of live context — what the model still sees.",
            "minimum": 0
          },
          "in_flight_bytes": {
            "type": "integer",
            "format": "int64",
            "description": "Bytes of the per-turn wire snapshot each running turn publishes so the\nconversation stays readable while the executor owns its state.",
            "minimum": 0
          },
          "messages": {
            "type": "integer",
            "minimum": 0
          },
          "resident": {
            "type": "integer",
            "description": "Conversations held in memory right now.",
            "minimum": 0
          },
          "turns_in_flight": {
            "type": "integer",
            "description": "Turns in flight, counted as the sessions holding an in-flight snapshot.",
            "minimum": 0
          },
          "unmeasured": {
            "type": "integer",
            "description": "Sessions skipped because a turn held their lock. Their bytes are missing\nfrom the totals below — a non-zero value here means the numbers are a\nfloor, not a total.",
            "minimum": 0
          }
        }
      },
      "ChatMessageWire": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "content",
              "role"
            ],
            "properties": {
              "content": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "enum": [
                  "user"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "content",
              "role"
            ],
            "properties": {
              "content": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "enum": [
                  "assistant"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A reasoning item preserved so it can be replayed with its tool call on a\nlater turn (Responses API requirement for reasoning models). Persisted so\nthe pairing survives a reload — dropping it would re-trigger the \"function\ncall without required reasoning item\" 400 on resume.",
            "required": [
              "id",
              "encrypted",
              "role"
            ],
            "properties": {
              "encrypted": {
                "type": "string"
              },
              "id": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "enum": [
                  "reasoning"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "id",
              "name",
              "args",
              "role"
            ],
            "properties": {
              "args": {},
              "call_id": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "id": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "enum": [
                  "tool_call"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "id",
              "name",
              "result",
              "role"
            ],
            "properties": {
              "call_id": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "id": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "result": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "enum": [
                  "tool_result"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A conversation boundary: everything above it is history the model no\nlonger sees.\n\nThe only wire message with no `AgentMessage` counterpart, and that is the\nwhole point of it. A reset ends a *context*, not a session — the transcript\nkeeps everything and the context restarts here.\n\nDistinct from the other way a context gets cleared, which is a *new\nsession* under the same agent (what a gateway conversation does after a\nquiet gap). That one leaves no divider because there is nothing to divide:\nit is a different conversation. This is for a clean slate inside the\nconversation you are already in. `reason` is short free text shown on the\ndivider, so a transcript can answer \"why does my history stop here\".",
            "required": [
              "at",
              "reason",
              "role"
            ],
            "properties": {
              "at": {
                "type": "string"
              },
              "reason": {
                "type": "string"
              },
              "role": {
                "type": "string",
                "enum": [
                  "reset"
                ]
              }
            }
          }
        ],
        "description": "Wire form for `metalcraft::AgentMessage` — the in-memory enum isn't\n`Serialize`, so we convert before responding. Also used as the on-disk\nformat for persisted chats, so it derives `Deserialize` too."
      },
      "ChatQueued": {
        "type": "object",
        "description": "The answer to a turn request that arrived mid-turn: taken, not started.",
        "required": [
          "queued",
          "position"
        ],
        "properties": {
          "position": {
            "type": "integer",
            "description": "Place in line, 1 for the next message to run.",
            "minimum": 0
          },
          "queued": {
            "type": "boolean",
            "description": "Always true. Present so a client can branch on the body rather than on\nthe status code, which is easy to lose through a wrapper."
          }
        }
      },
      "ChatSummary": {
        "type": "object",
        "required": [
          "id",
          "persona_slug",
          "model_name",
          "created_at",
          "turn_count"
        ],
        "properties": {
          "created_at": {
            "type": "string"
          },
          "flow_run": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/FlowRunRef",
                "description": "Set when this conversation is a flow run rather than something typed —\nwhich flow, and which run. A session list uses it to say so; without it a\nrow of an automation's output is indistinguishable from a chat, and the\nagent screen of a flow agent reads as a stack of anonymous timestamps."
              }
            ]
          },
          "id": {
            "type": "string"
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent this conversation belongs to. `None` only on records written before\ninstances existed, until the startup backfill reaches them.\n\nExposed because a client cannot group conversations by agent without it —\nwhich is the whole shape of the chat list once agents exist, and the summary\nwas the one place it was missing."
          },
          "model_name": {
            "type": "string"
          },
          "persona_slug": {
            "type": "string"
          },
          "preview": {
            "type": [
              "string",
              "null"
            ],
            "description": "The start of the *last* thing said, trimmed to one line — what makes a\nrow identifiable as *this* conversation rather than a timestamp beside an\nid, and what tells you where it got to."
          },
          "turn_count": {
            "type": "integer",
            "description": "How many times the user spoke. The unit a person counts a conversation in\n— not messages, which counts the agent's tool chatter, and not the\ntranscript length, which now also counts reset dividers.",
            "minimum": 0
          },
          "updated_at": {
            "type": [
              "string",
              "null"
            ],
            "description": "Last activity, from the chat file's mtime — [`persist_chat`] rewrites it\nafter every turn, so it is the clock the pod already trusts for staleness\n(see [`gateway_session_is_stale`]). `None` when the file cannot be read.\n\nA list sorted by `created_at` puts a conversation someone has been in all\nday below one they opened this morning and abandoned, which is the wrong\nway round for the question a session list answers."
          }
        }
      },
      "ChatTurnRequest": {
        "type": "object",
        "required": [
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          }
        }
      },
      "Connection": {
        "type": "object",
        "description": "What this pod is to one registry.",
        "required": [
          "registry",
          "url",
          "trust",
          "state"
        ],
        "properties": {
          "account": {
            "type": [
              "string",
              "null"
            ],
            "description": "Whatever the host will say about the account — an email, usually."
          },
          "detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "The host's own words when something went wrong, for a UI to show verbatim\ninstead of inventing an explanation."
          },
          "link_url": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where a human goes to finish the connection. Taken from the host's own answer\nrather than assembled here: a URL we guessed is a URL we would still be sending\npeople to after the host moved it."
          },
          "registry": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/ConnectionState"
          },
          "token_key": {
            "type": [
              "string",
              "null"
            ],
            "description": "The key-store entry this registry draws its bearer from — the name, never the\nvalue."
          },
          "trust": {
            "$ref": "#/components/schemas/Trust"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "ConnectionState": {
        "type": "string",
        "description": "How far a registry connection got, in the only terms a UI can act on.",
        "enum": [
          "connected",
          "unlinked",
          "no-token",
          "rejected",
          "unsupported",
          "unreachable"
        ]
      },
      "ConsentSummary": {
        "type": "object",
        "description": "What a human is shown before approving an install.",
        "required": [
          "domains",
          "requires_env",
          "tools",
          "mutating_tools"
        ],
        "properties": {
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Every origin this agent's tools can reach."
          },
          "mutating_tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tools classified as mutating. A read-only agent is a materially smaller\ncommitment than one that can write, and the dialog should say which it is."
          },
          "requires_env": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EnvRequirement"
            },
            "description": "Every credential it wants, and what wants it."
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tool names it gains, so \"what can this thing actually do\" is answerable."
          }
        }
      },
      "Counters": {
        "type": "object",
        "description": "What the project has spent and how far it has got.",
        "properties": {
          "compute_minutes_used": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "last_tick_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "no_progress_streak": {
            "type": "integer",
            "format": "int32",
            "description": "Consecutive ticks that changed nothing. Drives the rail, and (later) the\nmodel-tier escalation.",
            "minimum": 0
          },
          "ticks": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "tokens_spent": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "CreateChannelRequest": {
        "type": "object",
        "required": [
          "name",
          "url",
          "secret"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "secret": {
            "type": "string"
          },
          "slug": {
            "type": [
              "string",
              "null"
            ]
          },
          "url": {
            "type": "string"
          }
        }
      },
      "CreateChatRequest": {
        "type": "object",
        "properties": {
          "agent_preset": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent to start this conversation with. Defaults to `general-agent`."
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Continue an existing agent instead of minting a new one — how a named agent\naccumulates conversations."
          },
          "model_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Name the agent. A label only — nothing about an agent's lifetime follows\nfrom it, and nothing deletes an agent on a timer."
          },
          "persona_slug": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional now: with an `agent_preset`, the persona comes from the preset's\ndefault. Kept for callers that still pick a persona directly."
          }
        }
      },
      "CreateInstanceRequest": {
        "type": "object",
        "properties": {
          "agent_preset": {
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "CreateProjectRequest": {
        "type": "object",
        "required": [
          "goal"
        ],
        "properties": {
          "agent_preset": {
            "type": [
              "string",
              "null"
            ]
          },
          "branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "The branch the project works on. Defaults to `project/<id>`."
          },
          "chat_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The chat to send questions and reports to."
          },
          "every_minutes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Minutes between ticks. Floored at 5; defaults to 30.",
            "minimum": 0
          },
          "goal": {
            "type": "string",
            "description": "**The goal.** Room for a paragraph: this is the whole instruction, and a\ntick six days from now reads it verbatim."
          },
          "kind": {
            "$ref": "#/components/schemas/ProjectKind"
          },
          "models": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ModelTiers"
              }
            ]
          },
          "paused": {
            "type": "boolean",
            "description": "Create it paused, to review the plan before it starts spending."
          },
          "rails": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Rails"
              }
            ]
          },
          "repo": {
            "type": [
              "string",
              "null"
            ],
            "description": "The repo to work in, `owner/name`. Optional in this phase — a project\nwithout one still plans and thinks, it just has nowhere to build."
          },
          "timezone": {
            "type": [
              "string",
              "null"
            ]
          },
          "title": {
            "type": [
              "string",
              "null"
            ],
            "description": "Short handle for lists. Defaults to the first line of the project."
          }
        }
      },
      "CreateScheduledFlowRequest": {
        "type": "object",
        "required": [
          "flow_id",
          "schedule"
        ],
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Start firing immediately. Defaults to `true` — creating a schedule is the\nact of asking for it; a client staging one passes `false`."
          },
          "flow_id": {
            "type": "string",
            "description": "The flow to run."
          },
          "from_suggestion": {
            "type": [
              "string",
              "null"
            ],
            "description": "The author's suggestion key, when the person accepted a suggested schedule\nfrom a pack or the registry."
          },
          "id": {
            "type": [
              "string",
              "null"
            ],
            "description": "A hand-chosen id instead of a generated one. Rejected on collision: a create\nmust never overwrite an existing schedule."
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Attach to an existing agent instead of minting one — e.g. run the briefer as\nthe same agent you chat with."
          },
          "schedule": {
            "$ref": "#/components/schemas/ScheduleSpecDoc",
            "description": "When to run it: `{ \"type\": \"cron\", \"cron\": \"…\", \"timezone\": \"…\" }`."
          }
        }
      },
      "DependencyReport": {
        "type": "object",
        "description": "What a flow needs beyond itself, checked against this agent's current state.",
        "required": [
          "required_packs",
          "missing_packs",
          "version_conflicts",
          "required_tools",
          "required_personas",
          "missing_personas",
          "required_env"
        ],
        "properties": {
          "missing_packs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Of `required_packs`, those not installed *and enabled* on this agent."
          },
          "missing_personas": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Of `required_personas`, those not available on this agent."
          },
          "required_env": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Secret keys the required (installed) packs declare in their `requires_env`."
          },
          "required_packs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Integrations the flow requires — the union of what the graph\nreferences (`sub_agent.pack` + custom vendor nodes) and what the flow's\n`requires` block declares."
          },
          "required_personas": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Personas the flow's nodes run as (`data.persona`)."
          },
          "required_tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tool names the flow's `tool` nodes invoke (the real API surface)."
          },
          "version_conflicts": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Version/hash conflicts: a required pack is installed & enabled but its\nversion is outside the declared range, or its content hash doesn't match a\npin. Human-readable, one per conflict (advisory — install never hard-fails)."
          }
        }
      },
      "DiagnosticsSession": {
        "type": "object",
        "description": "A fully reconstructed session: its `session_info.json` plus an ordered\ntimeline of every other JSON event file in the directory.",
        "required": [
          "id",
          "timeline"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "session_info": {},
          "timeline": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TimelineEvent"
            }
          }
        }
      },
      "DiagnosticsSessionSummary": {
        "type": "object",
        "description": "One session in the listing (metadata + turn count, no per-turn payloads).",
        "required": [
          "id",
          "timestamp",
          "turn_count"
        ],
        "properties": {
          "flow_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Present (and `kind == \"flow\"`) when produced by a flow run."
          },
          "id": {
            "type": "string"
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent this session belongs to. Absent on sessions written before agents\nexisted, and on CLI runs, which have no agent.\n\nThis is what lets a Sessions list answer \"which agent produced this?\" — the\nquestion that matters most for a background agent, whose failures arrive here\nwith nobody watching."
          },
          "kind": {
            "type": [
              "string",
              "null"
            ],
            "description": "\"session\" for a normal run, \"flow\" for a flow run."
          },
          "model_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "persona_slug": {
            "type": [
              "string",
              "null"
            ]
          },
          "timestamp": {
            "type": "string"
          },
          "turn_count": {
            "type": "integer",
            "description": "Count of `turn_NNN.json` files — how far the run actually got.",
            "minimum": 0
          }
        }
      },
      "DreamReport": {
        "type": "object",
        "description": "One run of the dream, for one agent. Written to\n`<data>/memory/instances/<id>/dreams/<ts>.json` and returned by the on-demand\nentry points.",
        "required": [
          "instance_id",
          "trigger",
          "model",
          "started_at",
          "finished_at",
          "stages",
          "memories_before",
          "memories_after",
          "captures_pending_before",
          "captures_pending_after",
          "snapshot_written"
        ],
        "properties": {
          "captures_pending_after": {
            "type": "integer",
            "minimum": 0
          },
          "captures_pending_before": {
            "type": "integer",
            "minimum": 0
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "finished_at": {
            "type": "string",
            "format": "date-time"
          },
          "instance_id": {
            "type": "string"
          },
          "memories_after": {
            "type": "integer",
            "minimum": 0
          },
          "memories_before": {
            "type": "integer",
            "description": "Live memories before and after, so the run's net effect is one subtraction\nrather than a sum over stage counts that double-count merges.",
            "minimum": 0
          },
          "model": {
            "type": "string"
          },
          "snapshot_written": {
            "type": "boolean",
            "description": "Whether the log was folded into a fresh snapshot at the end."
          },
          "stages": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StageReport"
            }
          },
          "started_at": {
            "type": "string",
            "format": "date-time"
          },
          "trigger": {
            "type": "string"
          }
        }
      },
      "DreamRequest": {
        "type": "object",
        "properties": {
          "stages": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            },
            "description": "Which stages to run: 1 index, 2 consolidate, 3 abstract, 4 associate,\n5 decay. Omit for the configured set (all five by default); `[1, 5]` is the\nfast mechanical pass with no model calls."
          }
        }
      },
      "DreamStatus": {
        "type": "object",
        "description": "When the dream runs, and what happened the last time it did.",
        "required": [
          "nightly_enabled",
          "cron",
          "model",
          "active_days"
        ],
        "properties": {
          "active_days": {
            "type": "integer",
            "format": "int64",
            "description": "How recently an agent must have been used to be swept."
          },
          "cron": {
            "type": "string",
            "description": "The six-field cron it fires on."
          },
          "last_error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why the last run failed, when it did."
          },
          "last_run_at": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "last_summary": {
            "type": [
              "string",
              "null"
            ],
            "description": "One line on what the last run did, ready to print."
          },
          "last_trigger": {
            "type": [
              "string",
              "null"
            ],
            "description": "`\"nightly\"`, `\"catchup\"` or `\"manual\"` — how the last run started."
          },
          "model": {
            "type": "string",
            "description": "The model the thinking stages use."
          },
          "next_run_at": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "nightly_enabled": {
            "type": "boolean",
            "description": "Whether the nightly schedule is armed (`MEMORY_DREAM`). On-demand runs work\neither way — turning off a schedule is not removing the capability."
          },
          "timezone": {
            "type": [
              "string",
              "null"
            ],
            "description": "The zone that cron is read in; absent means the pod's own clock."
          }
        }
      },
      "EnvRequirement": {
        "type": "object",
        "description": "One credential, attributed to what needs it — because the question a human is\nanswering at install time is \"which secrets do I have to paste in\", not \"which\npack wants what\".",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "needed_by": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "required": {
            "type": "boolean"
          }
        }
      },
      "ErrorResponse": {
        "type": "object",
        "required": [
          "error"
        ],
        "properties": {
          "error": {
            "type": "string"
          }
        }
      },
      "Evidence": {
        "type": "object",
        "required": [
          "kind",
          "value",
          "at"
        ],
        "properties": {
          "at": {
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/EvidenceKind"
          },
          "value": {
            "type": "string"
          }
        }
      },
      "EvidenceKind": {
        "type": "string",
        "description": "What kind of proof a task finished.",
        "enum": [
          "commit",
          "run",
          "finding",
          "file",
          "note"
        ]
      },
      "ExportAgentPackRequest": {
        "type": "object",
        "required": [
          "preset"
        ],
        "properties": {
          "out": {
            "type": [
              "string",
              "null"
            ],
            "description": "Write the archive here. Omit to receive the bytes in the response."
          },
          "preset": {
            "type": "string"
          },
          "version": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "FactoryResetRequest": {
        "type": "object",
        "description": "Body for `POST /api/v1/factory-reset`.",
        "required": [
          "confirm"
        ],
        "properties": {
          "confirm": {
            "type": "string",
            "description": "Must be exactly `FACTORY RESET`. A typed phrase rather than a flag, so\nthat nothing reaches this endpoint by accident — see\n[`crate::factory_reset::CONFIRM_PHRASE`]."
          },
          "scope": {
            "$ref": "#/components/schemas/ResetScope",
            "description": "Defaults to [`ResetScope::Full`], the scope that actually reproduces a\nnew pod. `keep_keys` is the convenience option and the weaker test."
          }
        }
      },
      "Finding": {
        "type": "object",
        "required": [
          "id",
          "title",
          "severity",
          "state",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "created_at": {
            "type": "string"
          },
          "detail": {
            "type": "string"
          },
          "file": {
            "type": [
              "string",
              "null"
            ],
            "description": "`path/to/file.rs:42`, when it has one."
          },
          "id": {
            "type": "string",
            "description": "Short and stable (`f1`, `f2`) — it is quoted in PR bodies and in the\nscratchpad, so it has to be typeable."
          },
          "link": {
            "type": [
              "string",
              "null"
            ],
            "description": "The PR or issue it turned into."
          },
          "severity": {
            "$ref": "#/components/schemas/Severity"
          },
          "state": {
            "$ref": "#/components/schemas/FindingState"
          },
          "title": {
            "type": "string"
          },
          "updated_at": {
            "type": "string"
          }
        }
      },
      "FindingState": {
        "type": "string",
        "description": "Where a finding has got to.\n\n`Rejected` is the state that keeps the ledger honest. Without it a finding\nsomebody decided against would be re-found on the next sweep, forever — the\nledger has to remember the noes as well as the yeses.",
        "enum": [
          "open",
          "pr_open",
          "issue_open",
          "merged",
          "rejected"
        ]
      },
      "FlowBindingView": {
        "type": "object",
        "description": "The binding for one flow, resolved for display.",
        "required": [
          "flow_id",
          "preset",
          "bound",
          "personas",
          "armed",
          "consent"
        ],
        "properties": {
          "armed": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ArmedSchedule"
            },
            "description": "The schedules of this flow that are armed to an agent."
          },
          "bound": {
            "type": "boolean",
            "description": "True when the preset was chosen deliberately rather than defaulted."
          },
          "consent": {
            "$ref": "#/components/schemas/ArmConsent",
            "description": "Everything the arm dialog needs to state what arming this actually permits.\n\nArming is the second consent moment, and the sharper one: a scheduled flow\nacts **while nobody is watching**, so a mutating tool inside one is a bigger\ncommitment than the same tool in a chat where an approval prompt exists."
          },
          "flow_id": {
            "type": "string"
          },
          "personas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowPersonaCheck"
            },
            "description": "Personas the flow names, and whether the preset can reach each one."
          },
          "preset": {
            "type": "string",
            "description": "The preset the flow runs as. Always populated — an unbound flow resolves to\nthe default agent, which is what it effectively already was."
          }
        }
      },
      "FlowDefinition": {
        "type": "object",
        "description": "A graph: nodes and the directed edges between them.",
        "required": [
          "nodes",
          "edges"
        ],
        "properties": {
          "edges": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowEdge"
            },
            "description": "All directed arcs in the graph."
          },
          "nodes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowNode"
            },
            "description": "All vertices in the graph."
          }
        }
      },
      "FlowEdge": {
        "type": "object",
        "description": "A directed arc connecting two nodes in a [`FlowDefinition`].",
        "required": [
          "id",
          "source",
          "target"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Unique identifier within the enclosing [`FlowDefinition`]."
          },
          "source": {
            "type": "string",
            "description": "The id of the source [`FlowNode`]."
          },
          "source_handle": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional named output port on the source node (multi-output nodes\nlike [`CoreNodeType::Branch`])."
          },
          "target": {
            "type": "string",
            "description": "The id of the target [`FlowNode`]."
          },
          "target_handle": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional named input port on the target node."
          }
        }
      },
      "FlowList": {
        "type": "object",
        "required": [
          "flows"
        ],
        "properties": {
          "flows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowListItem"
            }
          }
        }
      },
      "FlowListItem": {
        "type": "object",
        "description": "One flow, resolved for display.\n\nA flow is *what work is this*: the graph, the agent preset it runs as, and how\nmany schedules point at it. **When** it runs is not here — that is\n`GET /api/v1/scheduled-flows`, one call for the whole pod, which a client joins\nagainst this listing by `flow_id`.",
        "required": [
          "id",
          "name",
          "node_count",
          "created_at",
          "updated_at",
          "v2",
          "preset",
          "scheduled_count",
          "enabled_count"
        ],
        "properties": {
          "created_at": {
            "type": "string"
          },
          "enabled_count": {
            "type": "integer",
            "description": "Of `scheduled_count`, how many are enabled. Zero means nothing fires.",
            "minimum": 0
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "node_count": {
            "type": "integer",
            "minimum": 0
          },
          "preset": {
            "type": "string",
            "description": "The preset this flow runs as. Always populated — an unbound flow resolves to\nthe default agent, which is what it effectively already was."
          },
          "scheduled_count": {
            "type": "integer",
            "description": "How many schedules point at this flow, of which how many are enabled.\n\nEnough for a listing to say \"runs twice a day\" or \"never runs\" without\nfetching every schedule; a client that wants the triggers themselves reads\n`/scheduled-flows`.",
            "minimum": 0
          },
          "updated_at": {
            "type": "string"
          },
          "v2": {
            "type": "boolean",
            "description": "v2 flows run on the state-machine executor and `POST /run` answers with a\n`FlowRunSummary`; v1 flows answer with the legacy per-prompt response. Worth\nknowing before offering a button that has to render one of them."
          }
        }
      },
      "FlowNode": {
        "type": "object",
        "description": "A single vertex in a [`FlowDefinition`].",
        "required": [
          "id",
          "node_type",
          "data"
        ],
        "properties": {
          "data": {
            "description": "Free-form per-node configuration. Schema depends on `node_type`."
          },
          "id": {
            "type": "string",
            "description": "Unique identifier within the enclosing [`FlowDefinition`]."
          },
          "node_type": {
            "$ref": "#/components/schemas/FlowNodeType",
            "description": "The node kind. See [`FlowNodeType`]."
          },
          "position": {
            "type": "array",
            "items": {
              "type": "number",
              "format": "double"
            },
            "description": "`[x, y]` coordinates for visual editors. Defaults to `[0.0, 0.0]`."
          }
        }
      },
      "FlowNodeType": {
        "type": "string",
        "description": "A core node type (entry, prompt, conditional, branch, set_variable, tool, http, sub_agent, approval, wait, foreach, end, branch_tool) or a vendor-namespaced custom type such as `slack:send_message`. Custom types are opaque and must round-trip unchanged.",
        "examples": [
          "prompt"
        ]
      },
      "FlowPersonaCheck": {
        "type": "object",
        "required": [
          "slug",
          "allowed"
        ],
        "properties": {
          "allowed": {
            "type": "boolean"
          },
          "slug": {
            "type": "string"
          }
        }
      },
      "FlowPromptResult": {
        "type": "object",
        "description": "Result of running one prompt node of a flow.",
        "required": [
          "prompt_index",
          "status"
        ],
        "properties": {
          "answer": {
            "type": [
              "string",
              "null"
            ]
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "prompt_index": {
            "type": "integer",
            "minimum": 0
          },
          "status": {
            "type": "string",
            "description": "\"completed\" | \"interrupted\" | \"failed\"."
          }
        }
      },
      "FlowRun": {
        "type": "object",
        "description": "A persisted flow run.",
        "required": [
          "id",
          "flow_id",
          "status",
          "current_node_id",
          "variables",
          "persona",
          "model",
          "cwd",
          "steps",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "chat_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The conversation this run is writing itself into, so a resumed run\ncontinues the thread it paused in rather than starting a stranger's.\nAbsent when the run has no agent (nothing to record into) or never reached\na node that spoke."
          },
          "created_at": {
            "type": "string",
            "description": "RFC-3339 creation timestamp."
          },
          "current_node_id": {
            "type": "string",
            "description": "The node the run is paused at (for `paused`) or last ran."
          },
          "cwd": {
            "type": "string",
            "description": "Working directory to resume in."
          },
          "flow": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SavedFlow",
                "description": "Snapshot of the flow definition at pause time, so resume routes against\nthe graph the run actually paused in — not a since-edited on-disk flow.\nAbsent (`None`) on legacy records; resume then falls back to loading the\ncurrent flow."
              }
            ]
          },
          "flow_id": {
            "type": "string",
            "description": "The flow this run belongs to."
          },
          "id": {
            "type": "string",
            "description": "Unique run id (the `runs/{id}.json` filename)."
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent instance this run belongs to, when a schedule armed one. Pod-local\nand never published — see [`crate::flow_bindings`]. Absent on ad-hoc runs and\non records written before agent instances existed, which resume as before."
          },
          "model": {
            "type": "string",
            "description": "Model name to resume with."
          },
          "pause": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PauseInfo",
                "description": "Pause details when `status == \"paused\"`."
              }
            ]
          },
          "persona": {
            "type": "string",
            "description": "Persona/model/cwd needed to resume the run."
          },
          "status": {
            "type": "string",
            "description": "`running` | `paused` | `completed` | `failed`."
          },
          "steps": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowStep"
            },
            "description": "The trace accumulated so far."
          },
          "updated_at": {
            "type": "string",
            "description": "RFC-3339 last-update timestamp."
          },
          "variables": {
            "type": "object",
            "description": "The run's state (`variables`) at the checkpoint."
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Non-fatal warnings computed when the run started — e.g. required packs/personas\nthat aren't installed or enabled. Surfaced in flow-debug UIs so a run that can't\nfully work says why. Empty (and omitted) when the flow has everything it needs."
          }
        }
      },
      "FlowRunRef": {
        "type": "object",
        "description": "Which run of which flow a conversation is.\n\nStamped on the chat a run writes itself into, so a session list can say\n\"this is a run of Morning brief\" rather than showing a timestamp and a\npreview, and so the pod can tell an automation's own transcripts apart from\nthe ones a person typed into the same agent.",
        "required": [
          "flow_id",
          "run_id"
        ],
        "properties": {
          "flow_id": {
            "type": "string"
          },
          "run_id": {
            "type": "string",
            "description": "The `run_id` of [`crate::flow_exec::FlowRunSummary`] — the same id the\n`runs/{id}.json` record carries when the run paused."
          }
        }
      },
      "FlowRunSummary": {
        "type": "object",
        "description": "The result of running (or pausing) a flow.",
        "required": [
          "run_id",
          "flow_id",
          "status",
          "steps",
          "variables"
        ],
        "properties": {
          "chat_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The conversation this run wrote itself into, so a caller can link a run\nstraight to its transcript. Absent for a run with no agent, and for a\ntool-only flow that never spoke."
          },
          "flow_id": {
            "type": "string",
            "description": "The flow that ran."
          },
          "run_id": {
            "type": "string",
            "description": "The run id (matches the `runs/{id}.json` record when the run paused)."
          },
          "status": {
            "type": "string",
            "description": "`completed` | `failed` | `paused`."
          },
          "steps": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowStep"
            },
            "description": "Per-node trace, in execution order."
          },
          "variables": {
            "type": "object",
            "description": "Final (or checkpointed) state — the `variables` object."
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Missing-dependency warnings for this run (empty when the flow has everything\nit needs). Shown in flow-debug UIs."
          }
        }
      },
      "FlowStep": {
        "type": "object",
        "description": "One node's contribution to the run trace.",
        "required": [
          "node_id",
          "node_type",
          "outcome"
        ],
        "properties": {
          "detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional human-readable detail (answer snippet, error, chosen handle)."
          },
          "node_id": {
            "type": "string",
            "description": "The node that ran."
          },
          "node_type": {
            "type": "string",
            "description": "Its wire-format type."
          },
          "outcome": {
            "type": "string",
            "description": "`advanced` | `routed:<handle>` | `completed` | `failed`."
          }
        }
      },
      "FlowTemplate": {
        "type": "object",
        "required": [
          "slug",
          "name",
          "flow"
        ],
        "properties": {
          "flow": {
            "description": "The raw template flow JSON, ready to be cloned and edited as a new flow."
          },
          "name": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "slug": {
            "type": "string"
          }
        }
      },
      "FlowTemplateSummary": {
        "type": "object",
        "required": [
          "slug",
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "slug": {
            "type": "string"
          }
        }
      },
      "FlowValidation": {
        "type": "object",
        "description": "What is wrong with a flow graph, without saving it.",
        "required": [
          "valid",
          "errors"
        ],
        "properties": {
          "errors": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "One sentence per problem, in the validator's own words."
          },
          "valid": {
            "type": "boolean",
            "description": "True when the graph would save. An editor enables its save button on this\nrather than on an empty `errors`, so a future non-fatal warning does not\nsilently start blocking saves."
          }
        }
      },
      "GatewayEvent": {
        "type": "object",
        "description": "One inbound or outbound gateway event.\n\n`channel_id` is `None` when an inbound message matched no enabled channel —\nthese \"unrouted\" records are what make the global Network view useful for\ndiagnosing misconfiguration (e.g. a wrong `integration_id`).",
        "required": [
          "ts",
          "direction",
          "platform",
          "outcome"
        ],
        "properties": {
          "body": {
            "type": "string",
            "description": "Message body (truncated to [`MAX_BODY_CHARS`])."
          },
          "channel_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The matched gateway channel's id, or `None` if unrouted."
          },
          "channel_name": {
            "type": [
              "string",
              "null"
            ],
            "description": "The matched gateway channel's name, for display."
          },
          "detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "Extra context — persona used, error message, etc."
          },
          "direction": {
            "type": "string",
            "description": "`\"inbound\"` or `\"outbound\"`."
          },
          "from": {
            "type": [
              "string",
              "null"
            ],
            "description": "Sender identifier (phone number for WhatsApp)."
          },
          "from_name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Human-readable sender name, when known."
          },
          "outcome": {
            "type": "string",
            "description": "Outcome: `routed`, `no_matching_channel`, `signature_rejected`,\n`sent`, `send_failed`."
          },
          "platform": {
            "type": "string",
            "description": "Delivery kind, e.g. `\"apns\"`, `\"whatsapp\"`, or `\"text\"`."
          },
          "source_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Upstream gateway integration UUID (`source_id`), when known."
          },
          "to": {
            "type": [
              "string",
              "null"
            ],
            "description": "Recipient identifier."
          },
          "ts": {
            "type": "string",
            "description": "RFC3339 timestamp; filled in by [`record`] if left empty."
          }
        }
      },
      "GatewayStatus": {
        "type": "object",
        "description": "What the workshop Connect panel renders from.",
        "required": [
          "configured",
          "registered",
          "verified",
          "connected",
          "streaming",
          "has_public_url",
          "webhook_stale"
        ],
        "properties": {
          "active_number": {
            "type": [
              "string",
              "null"
            ]
          },
          "channel": {
            "type": [
              "string",
              "null"
            ]
          },
          "configured": {
            "type": "boolean",
            "description": "The pod has a token (is linked to a Metalcraft ID account)."
          },
          "connected": {
            "type": "boolean",
            "description": "A local `metalcraft-gateway` channel is enabled and the webhook secret is set."
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "has_public_url": {
            "type": "boolean",
            "description": "Whether the pod knows its own public URL (POD_PUBLIC_URL) for the webhook."
          },
          "registered": {
            "type": "boolean",
            "description": "The user has registered a personal number on the gateway."
          },
          "streaming": {
            "type": "boolean",
            "description": "The Inbound Pull long-poll is currently connected and draining. Unlike `connected`\n(config present on disk), this is *intrinsic* liveness — it cannot be true while\ninbound delivery is actually dead. `false` in push mode (no long-poll runs)."
          },
          "verified": {
            "type": "boolean",
            "description": "That number is verified — required before connecting."
          },
          "webhook_stale": {
            "type": "boolean",
            "description": "The gateway's registered inbound webhook (`consumer_webhook_url`) no longer\npoints at this pod's current URL, so inbound messages are being delivered into\nthe void. `connected` can be true while this is true — the \"green light, dead\npipe\" state. Detecting it kicks off a reactive re-register (see `status`)."
          }
        }
      },
      "Heartbeat": {
        "type": "object",
        "description": "How often the project wakes.",
        "properties": {
          "conductor_minutes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "What the conductor has backed off to, when it has.\n\nThe conductor may slow itself down and it may come back, but it can never\ngo faster than the person set. That asymmetry is the whole design: a model\ndeciding how often to spend money is exactly the concern that got\nper-delegation model tiers deferred, and it disappears entirely if the\nonly direction it can move is *cheaper*. Backing off is judgement about\nthis project; spending more than was authorised is not its call.\n\nCleared when a person edits the period — a new instruction supersedes an\nold accommodation.",
            "minimum": 0
          },
          "every_minutes": {
            "type": "integer",
            "format": "int32",
            "description": "What the person asked for. The **fastest** this project may wake.",
            "minimum": 0
          },
          "timezone": {
            "type": [
              "string",
              "null"
            ],
            "description": "IANA name. Absent reads the pod's default, then UTC — the same rule\nscheduled flows follow."
          }
        }
      },
      "HttpApiToolConfig": {
        "type": "object",
        "description": "JSON config schema for a user-defined HTTP API tool.",
        "required": [
          "name",
          "description",
          "method",
          "url",
          "parameters"
        ],
        "properties": {
          "body_defaults": {
            "type": "object",
            "additionalProperties": {},
            "propertyNames": {
              "type": "string"
            }
          },
          "body_mapping": {
            "type": "string"
          },
          "body_template": {
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "type": "string"
          },
          "headers": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "propertyNames": {
              "type": "string"
            }
          },
          "method": {
            "type": "string"
          },
          "multipart": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/MultipartConfig",
                "description": "Present only when `body_mapping == \"multipart\"`: describes which argument\ncarries the local file path and what form-field name to send it under."
              }
            ]
          },
          "name": {
            "type": "string"
          },
          "param_paths": {
            "type": "object",
            "description": "Present only when `body_mapping == \"params_nested\"`: maps each flat\nargument name to a dotted JSON path in the request body (e.g.\n`\"prompt\": \"payload.structured_data.params.prompt\"`). Lets a tool expose\nsimple scalar parameters (which OpenAI's function-schema validation\naccepts) while still producing a deeply nested JSON body — without the\nstring-substitution hazards of `body_mapping == \"template\"`. Arguments\nwith no entry here are inserted at the top level under their own name.\nAbsent (optional) arguments are simply skipped, and `body_defaults`\nprovides the nested base the paths are written onto.",
            "additionalProperties": {
              "type": "string"
            },
            "propertyNames": {
              "type": "string"
            }
          },
          "parameters": {},
          "poll": {
            "type": "boolean",
            "description": "Marks this tool as a status **poll** (e.g. checking an async job until it\nfinishes). Polling means calling the same tool with the same arguments\nrepeatedly on purpose, which would otherwise look like a runaway loop, so\nthe step guard exempts poll tools from tight loop detection. See\n[`crate::guard`]."
          },
          "timeout_secs": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "How long to wait on this tool's HTTP request, in seconds. Defaults to\n[`DEFAULT_TIMEOUT_SECS`], which suits an API that answers in one round\ntrip and is far too short for one that holds the request open while it\nworks — a remote build, a clone, a container coming up. Those failed in\nthe *tool* while the server was still succeeding, which is the worst\nshape of failure: the agent sees an error for work that then completes.\nClamped to [`MAX_TIMEOUT_SECS`]; `0` and anything unparseable fall back\nto the default, so a typo cannot disable the bound.",
            "minimum": 0
          },
          "url": {
            "type": "string"
          }
        }
      },
      "InferenceStatus": {
        "type": "object",
        "description": "Whether this pod can run a turn, and on whose credential.\n\nThe one question a client cannot answer from `GET /api/v1/keys`: that endpoint\nlists `keys.json`, and a provisioned pod's credential is injected as container\nenv, so a healthy pod reads as an empty store. Clients that inferred \"no key,\ncannot think\" from it told people their working pod was dead. The pod is the\nonly honest source, so it answers here — through the same function a turn uses.",
        "required": [
          "ready",
          "credential",
          "gateway"
        ],
        "properties": {
          "base_url": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where inference is routed, userinfo and query stripped. Absent means the\ndefault, OpenAI proper."
          },
          "credential": {
            "type": "string",
            "description": "Which credential answered — `\"stored\"` (bound through this API),\n`\"environment\"` (injected by provisioning, or a `.env`), `\"pod_token\"`\n(this pod's own identity, offered only at the gateway), or `\"none\"`."
          },
          "gateway": {
            "type": "boolean",
            "description": "Routed at the Metalcraft gateway — so turns bill the account's credits."
          },
          "ready": {
            "type": "boolean",
            "description": "A credential resolves, so a turn has something to authenticate with. This\nis not a promise the turn *succeeds*: the gateway still meters credits and\nrequires the account's premium, which the pod cannot see."
          }
        }
      },
      "InstallDependenciesResponse": {
        "type": "object",
        "description": "Response for `POST /flows/{id}/check-dependencies`.",
        "required": [
          "flow",
          "packs"
        ],
        "properties": {
          "flow": {
            "type": "string",
            "description": "The flow whose dependencies were checked."
          },
          "packs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PackInstallOutcome"
            },
            "description": "One outcome per required pack."
          }
        }
      },
      "InstallFlowRequest": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string",
            "description": "Registry slug of the flow to install (equals the flow id)."
          }
        }
      },
      "InstallReport": {
        "type": "object",
        "description": "What an install actually did. Every field here is something a human or a UI has\na reason to see — silence about a skipped dependency is how you get an agent that\nhalf works.",
        "required": [
          "id",
          "version",
          "presets",
          "personas",
          "skills",
          "packs_stored",
          "packs_deduplicated",
          "missing_env",
          "preset_collisions",
          "memories_indexed",
          "consent"
        ],
        "properties": {
          "consent": {
            "$ref": "#/components/schemas/ConsentSummary"
          },
          "flows_installed_unscheduled": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Flows the pack shipped. **Every one lands disabled and unarmed** — installing\nan identity must not start background work, and arming is the separate consent\nmoment that mints the agent to run it."
          },
          "id": {
            "type": "string"
          },
          "memories_indexed": {
            "type": "integer",
            "minimum": 0
          },
          "missing_env": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Credentials the pod does not have yet. A **warning**, never a failure: the\npack installs, its tools error clearly at call time, and `key_set` fixes it."
          },
          "packs_deduplicated": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Packs already present under the same hash — free, because content-addressed."
          },
          "packs_stored": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Packs newly written to the content store."
          },
          "personas": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "preset_collisions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Preset slugs already provided by another installed pack."
          },
          "presets": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "version": {
            "type": "string"
          }
        }
      },
      "InstallResult": {
        "type": "object",
        "description": "The result of a successful install: what landed + what it still needs.",
        "required": [
          "flow",
          "dependencies"
        ],
        "properties": {
          "agent_preset": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent preset this flow was bound to, chosen because its roster covers\nevery persona the flow names. `None` means no installed preset can reach them\nall — the flow is saved and runnable by hand, but cannot be armed until one\ncan, which is worth saying at install rather than discovering later."
          },
          "dependencies": {
            "$ref": "#/components/schemas/DependencyReport"
          },
          "flow": {
            "$ref": "#/components/schemas/InstalledFlow"
          },
          "suggested_schedules": {
            "type": "array",
            "items": {
              "type": "object"
            },
            "description": "Schedules the author suggests for this flow. **Nothing was scheduled** —\ninstalling a flow starts no background work, which is now a property of the\nformat rather than a flag this path has to remember to clear. A client\noffers these; a person accepting one creates the schedule."
          }
        }
      },
      "InstalledAgentPack": {
        "type": "object",
        "description": "An installed agent pack.",
        "required": [
          "id",
          "manifest",
          "root"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "manifest": {
            "$ref": "#/components/schemas/AgentPackManifest"
          },
          "root": {
            "type": "string"
          }
        }
      },
      "InstalledFlow": {
        "type": "object",
        "description": "Summary of the flow that was written.",
        "required": [
          "id",
          "name",
          "node_count"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "node_count": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "InstanceDetail": {
        "allOf": [
          {
            "$ref": "#/components/schemas/AgentInstance"
          },
          {
            "type": "object",
            "required": [
              "conversations",
              "scheduled"
            ],
            "properties": {
              "conversations": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ChatSummary"
                }
              },
              "scheduled": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ScheduledFlowRow"
                },
                "description": "What this agent is scheduled to do — the schedules armed to it. A pod\ncould not previously answer that question about a background agent."
              }
            }
          }
        ]
      },
      "InstanceFlows": {
        "type": "object",
        "required": [
          "scheduled"
        ],
        "properties": {
          "scheduled": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ScheduledFlowRow"
            }
          }
        }
      },
      "InstanceList": {
        "type": "object",
        "required": [
          "instances"
        ],
        "properties": {
          "instances": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/InstanceListItem"
            }
          }
        }
      },
      "InstanceListItem": {
        "allOf": [
          {
            "$ref": "#/components/schemas/AgentInstance"
          },
          {
            "type": "object",
            "required": [
              "conversation_count"
            ],
            "properties": {
              "conversation_count": {
                "type": "integer",
                "minimum": 0
              }
            }
          }
        ],
        "description": "An agent in a list, with the one derived number a list actually needs.\n\nTyped rather than patched into a `serde_json::Value` so it reaches the spec —\n`conversation_count` was invisible to both generated clients, which is how a\nfield that exists on the wire can still be unusable."
      },
      "InstanceMemoryView": {
        "type": "object",
        "description": "What one agent knows, split by where it came from.",
        "required": [
          "instance_id",
          "shipped",
          "learned",
          "forgotten",
          "sample",
          "system"
        ],
        "properties": {
          "base": {
            "type": [
              "string",
              "null"
            ],
            "description": "`<preset>@<version>` when this agent was shipped a knowledge base."
          },
          "forgotten": {
            "type": "integer",
            "description": "Shipped memories this agent has been told to forget.",
            "minimum": 0
          },
          "instance_id": {
            "type": "string"
          },
          "learned": {
            "type": "integer",
            "description": "Memories this agent formed itself.",
            "minimum": 0
          },
          "sample": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MemorySample"
            }
          },
          "shipped": {
            "type": "integer",
            "description": "Memories the pack gave it, still visible (tombstones excluded).",
            "minimum": 0
          },
          "system": {
            "$ref": "#/components/schemas/MemorySystemStatus",
            "description": "How the machinery behind those numbers is configured and when it last ran.\n\nShipped with the view rather than on a route of its own because the\nquestion it answers — \"why does this agent know so little / so much?\" — is\nthe same question the counts above provoke, and two round trips to answer\none question is two chances to show a half-loaded pane."
          }
        }
      },
      "InstanceOrigin": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "workshop"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "cli"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A conversation with one person on one channel.\n\nKeyed by sender, not just by channel: an agent's memory is the continuity\nit exists to have, and one agent per *number* meant everything it learned\nabout everyone who texted in went into a single shared memory. `sender` is\nthe normalized key (see `gateway_sender_key`), not the raw `From` — the\nsame person must not get a second agent because their number arrived\nformatted differently.\n\n`None` on records written before this was per-sender; they keep working as\nthe channel's catch-all agent until they age out.",
            "required": [
              "channel",
              "kind"
            ],
            "properties": {
              "channel": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "gateway"
                ]
              },
              "sender": {
                "type": [
                  "string",
                  "null"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "flow_id",
              "kind"
            ],
            "properties": {
              "flow_id": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "flow"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An agent that exists to pursue one [project](crate::projects), for as long as\nthat takes.\n\nThe project is the reason the agent was minted, and the two live and die\ntogether in intent — but not in storage: deleting a project leaves the\ninstance, because what it learned along the way outlives the errand.",
            "required": [
              "project_id",
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "project"
                ]
              },
              "project_id": {
                "type": "string"
              },
              "role": {
                "$ref": "#/components/schemas/ProjectRole",
                "description": "Which of the project's two agents this is. A project runs a\n**conductor** (holds the plan, judges the goal) and a **worker**\n(does the work), and they must not share an instance: they\naccumulate different memory — the worker learns the codebase, the\nconductor learns the project — and merging the two would give each\nof them the other's recall at exactly the moments it is misleading.\n\nDefaults to `worker`, which is what a project created before the\nconductor existed had."
              }
            }
          }
        ],
        "description": "Where an instance came from.\n\nProvenance only, since agents are never deleted on a timer — it answers \"why\ndoes this exist\" (and, for `Gateway`, *who* it belongs to), not \"how long does\nit last\"."
      },
      "IntegrationDetail": {
        "type": "object",
        "required": [
          "id",
          "name",
          "description",
          "version",
          "enabled",
          "personas",
          "skills",
          "api_tools",
          "flow_templates"
        ],
        "properties": {
          "api_tools": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": {
            "type": "string"
          },
          "enabled": {
            "type": "boolean"
          },
          "flow_templates": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "personas": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "requires_env": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "version": {
            "type": "string"
          }
        }
      },
      "IntegrationRef": {
        "type": "object",
        "description": "A vendored integration, pinned by content.",
        "required": [
          "id",
          "version"
        ],
        "properties": {
          "content_sha256": {
            "type": [
              "string",
              "null"
            ],
            "description": "Integrity pin for the vendored copy. Verified at install."
          },
          "id": {
            "type": "string"
          },
          "source": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where the author obtained it, for provenance display. Never fetched from."
          },
          "version": {
            "type": "string"
          }
        }
      },
      "IntegrationSummary": {
        "type": "object",
        "required": [
          "id",
          "name",
          "description",
          "version",
          "enabled",
          "personas",
          "skills",
          "api_tools",
          "flow_templates"
        ],
        "properties": {
          "api_tools": {
            "type": "integer",
            "minimum": 0
          },
          "description": {
            "type": "string"
          },
          "enabled": {
            "type": "boolean"
          },
          "flow_templates": {
            "type": "integer",
            "minimum": 0
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "personas": {
            "type": "integer",
            "description": "Number of personas/skills/api_tools/flow_templates the pack provides.",
            "minimum": 0
          },
          "requires_env": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "skills": {
            "type": "integer",
            "minimum": 0
          },
          "version": {
            "type": "string"
          }
        }
      },
      "IoBinding": {
        "oneOf": [
          {
            "type": "object",
            "description": "A Workshop chat — deliver by appending to the chat and publishing to its\nlive event stream.",
            "required": [
              "chat_id",
              "kind"
            ],
            "properties": {
              "chat_id": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "workshop_chat"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A gateway conversation — deliver out through the bound adapter.",
            "required": [
              "channel_id",
              "address",
              "kind"
            ],
            "properties": {
              "address": {
                "type": "string"
              },
              "channel_id": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "gateway"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "No delivery channel known (e.g. a one-shot CLI run). The result is logged\nonly.",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "unbound"
                ]
              }
            }
          }
        ],
        "description": "Where a fired follow-up's reply should be delivered. Captured when the job is\narmed so the daemon can rebuild the right [`ReplySink`](crate::tools::ReplySink)."
      },
      "JournalEntry": {
        "type": "object",
        "description": "One line of what the project did, for a person to read.\n\nA structured record rather than chat messages: a tick summary is not a\nconversation turn, and the clients that render a project draw a progress bar and\na timeline from these fields. Questions for a person still go out through the\nproject's [`IoBinding`](crate::scheduled_tasks::IoBinding) — this is the log, not\nthe conversation.",
        "required": [
          "at",
          "tick",
          "kind",
          "model",
          "summary",
          "status",
          "plan_done",
          "plan_total",
          "progressed",
          "duration_secs"
        ],
        "properties": {
          "at": {
            "type": "string"
          },
          "briefing": {
            "type": [
              "string",
              "null"
            ],
            "description": "The briefing the conductor wrote for this tick. Recorded because the\nfirst question about a tick that went wrong is \"what did it actually\nsay?\", and that should be answerable without opening a transcript."
          },
          "conducted": {
            "type": "boolean",
            "description": "False when the conductor could not run and the tick used the fallback\nbriefing. \"The conductor was down for six ticks\" is the sort of thing\nthat explains a project which went nowhere."
          },
          "duration_secs": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "kind": {
            "$ref": "#/components/schemas/TickKind"
          },
          "model": {
            "type": "string"
          },
          "plan_done": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "plan_total": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "progressed": {
            "type": "boolean",
            "description": "False when the tick left the scratchpad byte-identical: it thought,\npossibly spent, and recorded nothing."
          },
          "status": {
            "$ref": "#/components/schemas/ProjectStatus",
            "description": "Status after the tick, so a reader can see where it turned."
          },
          "summary": {
            "type": "string",
            "description": "What the agent said it did — its final answer, trimmed."
          },
          "tick": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "KeyEntry": {
        "type": "object",
        "description": "A stored key with its **scope** and whether it is managed (platform- or\nconnection-owned → read-only in the UI). Returned by `GET /api/v1/keys` so\nthe Keys page can group global keys and per-channel secrets, and lock the\nmanaged ones. `channel_id`/`channel_name` are present only for channel scope.",
        "required": [
          "name",
          "masked",
          "scope",
          "managed"
        ],
        "properties": {
          "channel_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "channel_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "managed": {
            "type": "boolean"
          },
          "masked": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "scope": {
            "type": "string",
            "description": "`\"global\"` or `\"channel\"`."
          }
        }
      },
      "KeyRevealResponse": {
        "type": "object",
        "description": "The raw value of a key, returned only by the explicit reveal endpoint.",
        "required": [
          "value"
        ],
        "properties": {
          "value": {
            "type": "string"
          }
        }
      },
      "KeyScopeQuery": {
        "type": "object",
        "properties": {
          "channel_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "When set, target this channel's secret scope instead of global."
          }
        }
      },
      "KeySummary": {
        "type": "object",
        "description": "A stored API key, exposed to the workshop with its value masked — the\nraw secret is never sent over the wire. Used by the load-time snapshot and\nthe sidebar (global scope only).",
        "required": [
          "name",
          "masked"
        ],
        "properties": {
          "masked": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        }
      },
      "KeyValueBody": {
        "type": "object",
        "required": [
          "value"
        ],
        "properties": {
          "channel_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "When set, the key is written to this channel's secret scope instead of\nthe global namespace."
          },
          "value": {
            "type": "string"
          }
        }
      },
      "KindCount": {
        "type": "object",
        "required": [
          "kind",
          "count"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "minimum": 0
          },
          "kind": {
            "type": "string"
          }
        }
      },
      "Lock": {
        "type": "object",
        "description": "The lockfile document.",
        "properties": {
          "agent_packs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LockEntry"
            },
            "description": "Agent packs — the unit of installation. Self-contained, so restoring these\nneeds no dependency ordering: each carries its own integrations."
          },
          "flows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LockEntry"
            }
          },
          "packs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LockEntry"
            }
          },
          "version": {
            "type": "integer",
            "format": "int32",
            "description": "Lockfile format version (not the artifacts' versions).",
            "minimum": 0
          }
        }
      },
      "LockEntry": {
        "type": "object",
        "description": "One pinned artifact in the lockfile.",
        "required": [
          "name",
          "version",
          "content_sha256",
          "source"
        ],
        "properties": {
          "content_sha256": {
            "type": "string",
            "description": "Integrity hash of the installed content (verified on restore)."
          },
          "name": {
            "type": "string",
            "description": "Registry slug (== pack id / flow id)."
          },
          "source": {
            "type": "string",
            "description": "Registry origin the artifact came from (e.g. `https://packs.metalcraftai.com`)."
          },
          "version": {
            "type": "string",
            "description": "The concrete version installed."
          }
        }
      },
      "MemoriesRef": {
        "type": "object",
        "description": "Reference to the seed memories an agent pack ships with a preset.",
        "required": [
          "file"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "dims": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "minimum": 0
          },
          "embed_model": {
            "type": [
              "string",
              "null"
            ]
          },
          "file": {
            "type": "string"
          }
        }
      },
      "MemoryLimits": {
        "type": "object",
        "description": "The ceilings in force, echoed so an operator can see what a pod is actually\nrunning with rather than what the defaults say.",
        "required": [
          "max_tool_result_bytes",
          "max_request_body_bytes",
          "max_diagnostic_file_bytes"
        ],
        "properties": {
          "max_diagnostic_file_bytes": {
            "type": "integer",
            "minimum": 0
          },
          "max_request_body_bytes": {
            "type": "integer",
            "minimum": 0
          },
          "max_tool_result_bytes": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "MemoryMetrics": {
        "type": "object",
        "description": "Everything `GET /api/v1/metrics` reports.",
        "required": [
          "chats",
          "broadcasters",
          "counters",
          "limits"
        ],
        "properties": {
          "broadcasters": {
            "$ref": "#/components/schemas/BroadcasterMemory"
          },
          "chats": {
            "$ref": "#/components/schemas/ChatMemory"
          },
          "counters": {
            "$ref": "#/components/schemas/Counters"
          },
          "limits": {
            "$ref": "#/components/schemas/MemoryLimits"
          },
          "rss_bytes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Resident set size of the process. `null` off Linux, where it can't be\nread without a dependency this crate doesn't carry.",
            "minimum": 0
          }
        }
      },
      "MemorySample": {
        "type": "object",
        "required": [
          "id",
          "kind",
          "text",
          "importance",
          "origin",
          "tags"
        ],
        "properties": {
          "entity": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "type": "string"
          },
          "importance": {
            "type": "number",
            "format": "float"
          },
          "kind": {
            "type": "string"
          },
          "origin": {
            "type": "string",
            "description": "`\"shipped\"` or `\"learned\"` — the distinction the UI actually needs."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "text": {
            "type": "string"
          }
        }
      },
      "MemorySystemStatus": {
        "type": "object",
        "description": "The state of the memory subsystem for one agent: what is switched on, what is\nwaiting, and what the dream did about it last.",
        "required": [
          "enabled",
          "capture_enabled",
          "recall_enabled",
          "pending_captures",
          "by_kind",
          "links",
          "archived",
          "superseded",
          "dream"
        ],
        "properties": {
          "archived": {
            "type": "integer",
            "description": "Faded out by the decay pass, still on disk.",
            "minimum": 0
          },
          "by_kind": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/KindCount"
            },
            "description": "Live memories by kind, largest first."
          },
          "capture_enabled": {
            "type": "boolean",
            "description": "`MEMORY_CAPTURE` — whether turns are being written to the queue at all."
          },
          "dream": {
            "$ref": "#/components/schemas/DreamStatus"
          },
          "enabled": {
            "type": "boolean",
            "description": "`MEMORY_ENABLED`. Off means no recall, no capture, no dream."
          },
          "links": {
            "type": "integer",
            "description": "Edges in this agent's own memory graph.",
            "minimum": 0
          },
          "pending_captures": {
            "type": "integer",
            "description": "Turns captured but not yet distilled. A number that only grows means a\ndream that is not running.",
            "minimum": 0
          },
          "recall_enabled": {
            "type": "boolean",
            "description": "`MEMORY_RECALL` — whether recall is injected into each turn."
          },
          "superseded": {
            "type": "integer",
            "description": "Merged into another memory by the dream.",
            "minimum": 0
          }
        }
      },
      "MgConnectRequest": {
        "type": "object",
        "properties": {
          "connection_token": {
            "type": [
              "string",
              "null"
            ],
            "description": "Audience-scoped connection token from the k3 broker, adopted as the\nchannel's outbound API key. Omitted on the Workshop path."
          },
          "webhook_base": {
            "type": [
              "string",
              "null"
            ],
            "description": "Override for the pod's public URL when `POD_PUBLIC_URL` isn't injected."
          }
        }
      },
      "MgRegisterRequest": {
        "type": "object",
        "required": [
          "phone_number"
        ],
        "properties": {
          "phone_number": {
            "type": "string"
          }
        }
      },
      "ModelFloor": {
        "type": "object",
        "description": "A **capability floor**, not a model name.\n\nA hard `\"gpt-5.4\"` breaks on a pod that doesn't have it. Declare what the agent\nneeds and let the pod map that onto what it has; `prefer` is a hint, never a\nrequirement.",
        "properties": {
          "min_context": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "minimum": 0
          },
          "needs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "prefer": {
            "type": [
              "string",
              "null"
            ]
          },
          "tier": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "ModelTiers": {
        "type": "object",
        "description": "Model tier per tick kind, in tier names rather than model ids so a project\nwritten on one pod runs on another.",
        "properties": {
          "plan": {
            "type": [
              "string",
              "null"
            ]
          },
          "review": {
            "type": [
              "string",
              "null"
            ]
          },
          "work": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "MultipartConfig": {
        "type": "object",
        "description": "Config for a `multipart/form-data` upload tool. The argument named by\n`file_param` is treated as a local file path (constrained to the upload\nroot) and sent as the file part `file_field`; all other arguments become\ntext fields.",
        "required": [
          "file_param",
          "file_field"
        ],
        "properties": {
          "file_field": {
            "type": "string",
            "description": "Multipart field name the server expects the file under (e.g. `\"file\"`)."
          },
          "file_param": {
            "type": "string",
            "description": "Incoming argument that holds the local file path to upload."
          }
        }
      },
      "NewConversationRequest": {
        "type": "object",
        "properties": {
          "model_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "persona_slug": {
            "type": [
              "string",
              "null"
            ],
            "description": "Start this conversation as a specific persona from the agent's roster."
          }
        }
      },
      "OctaweaveConnection": {
        "type": "object",
        "required": [
          "url",
          "state",
          "pack_installed"
        ],
        "properties": {
          "account": {
            "type": [
              "string",
              "null"
            ],
            "description": "Whatever Octaweave will say about the account — an email or a name."
          },
          "detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "The host's own words when something went wrong, shown rather than paraphrased."
          },
          "link_url": {
            "type": [
              "string",
              "null"
            ],
            "description": "Where a human goes to finish it. Octaweave's own answer when it offers one,\nfalling back to its account page — the page that hosts the link button."
          },
          "pack_installed": {
            "type": "boolean",
            "description": "Whether the tools are actually installed. Connecting without the pack reaches\nnothing, and installing the pack without connecting authenticates as nobody —\na panel needs both facts to say which half is missing."
          },
          "state": {
            "$ref": "#/components/schemas/OctaweaveConnectionState"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "OctaweaveConnectionState": {
        "type": "string",
        "description": "How far this pod got with Octaweave, in the terms a panel can act on.\n\nNamed for its service rather than `ConnectionState`, because `utoipa` keys the\nOpenAPI document by type name: a second `Connection` does not conflict loudly, it\n**silently replaces** the first, and the generated client then types one endpoint\nagainst another endpoint's shape.",
        "enum": [
          "connected",
          "unlinked",
          "no-token",
          "unreachable"
        ]
      },
      "Orphaned": {
        "type": "object",
        "description": "One agent whose preset was withdrawn by an update.",
        "required": [
          "instance",
          "name",
          "agent_preset",
          "frozen"
        ],
        "properties": {
          "agent_preset": {
            "type": "string"
          },
          "frozen": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Personas and skills copied into the user-local layer so the agent still runs."
          },
          "instance": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        }
      },
      "PackInstallOutcome": {
        "type": "object",
        "description": "Outcome of trying to satisfy one pack requirement of a flow.",
        "required": [
          "pack",
          "status"
        ],
        "properties": {
          "detail": {
            "type": [
              "string",
              "null"
            ],
            "description": "Human-readable detail (why it was skipped/failed, or the resolved version)."
          },
          "pack": {
            "type": "string",
            "description": "The pack id."
          },
          "status": {
            "type": "string",
            "description": "`\"installed\" | \"already-satisfied\" | \"skipped\" | \"failed\"`."
          },
          "version": {
            "type": [
              "string",
              "null"
            ],
            "description": "The version installed (or already present), when known."
          }
        }
      },
      "PackRequirement": {
        "type": "object",
        "description": "A single integration-pack dependency.",
        "required": [
          "id"
        ],
        "properties": {
          "content_sha256": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional integrity lock: the exact canonical content hash (64 lowercase\nhex chars) the author resolved against. When set, a host may refuse to\ninstall a pack whose content hash differs."
          },
          "id": {
            "type": "string",
            "description": "Pack id / slug — the stable identity. Must match\n`^[a-z0-9][a-z0-9_-]{0,63}$`."
          },
          "optional": {
            "type": "boolean",
            "description": "When `true`, an unmet requirement is a warning rather than a hard failure —\nthe flow is expected to degrade gracefully."
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Human-readable reason, surfaced in an install/enable prompt."
          },
          "resolved_version": {
            "type": [
              "string",
              "null"
            ],
            "description": "The concrete version the authoring environment resolved to, recorded as a\nreproducibility hint (a lock). Advisory; hosts may re-resolve within\n[`version`](Self::version)."
          },
          "version": {
            "type": [
              "string",
              "null"
            ],
            "description": "Semver range the pack must satisfy, e.g. `\">=1.2.0, <2.0.0\"`. `None` means\nany installed version is acceptable."
          }
        }
      },
      "Parent": {
        "type": "object",
        "required": [
          "id",
          "version"
        ],
        "properties": {
          "content_sha256": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        }
      },
      "PatchInstanceRequest": {
        "type": "object",
        "properties": {
          "name": {
            "type": [
              "string",
              "null"
            ]
          },
          "persona": {
            "type": [
              "string",
              "null"
            ],
            "description": "Move within the preset's roster. Rejected if the persona isn't in it."
          }
        }
      },
      "PauseInfo": {
        "type": "object",
        "description": "Why a run is paused and what will resume it.",
        "required": [
          "reason",
          "resume_handles"
        ],
        "properties": {
          "message": {
            "type": [
              "string",
              "null"
            ],
            "description": "For `approval`: the (interpolated) prompt shown to the human."
          },
          "reason": {
            "type": "string",
            "description": "`\"approval\"` or `\"wait\"`."
          },
          "resume_handles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "For `approval`: the decision handles a human may choose. For `wait`:\ntypically `[\"after\"]`."
          },
          "wake_at": {
            "type": [
              "string",
              "null"
            ],
            "description": "For `wait`: RFC-3339 timestamp at/after which the run may resume."
          }
        }
      },
      "PendingRun": {
        "type": "object",
        "description": "A long-running command the last tick started and did not wait for.\n\n`buildr_build`/`buildr_test` keep advancing after the request returns, and a\ncold build outlives any sane tick — so a tick may end owing one of these, and\nthe next tick's first act is to read it. The heartbeat is the polling loop.",
        "required": [
          "workspace_id",
          "run_id",
          "started_at"
        ],
        "properties": {
          "run_id": {
            "type": "string"
          },
          "started_at": {
            "type": "string"
          },
          "what": {
            "type": "string",
            "description": "What was started, for the journal — \"cargo test\", \"npm run build\"."
          },
          "workspace_id": {
            "type": "string"
          }
        }
      },
      "Persona": {
        "type": "object",
        "required": [
          "name",
          "description",
          "tools",
          "system_prompt"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "integrations": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Integrations this persona is scoped to, by id (e.g. `\"linear\"`). Every\nHTTP-API tool an installed integration listed here provides is added to\nthe persona's tool set, so a persona can adopt a whole integration without\nenumerating each `<id>_*` tool by name. Combine with `tools` for native\ntools like `load_skill`. See [`Persona::resolved_tool_names`].\n\nReads `packs` too: that was the field's name until integrations stopped\nbeing installable, and personas carrying it are already on people's pods."
          },
          "max_run_secs": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "How long a `sub_agent` delegation to this persona may run, in seconds.\n\nThe delegation guard has to bound a runaway sub-agent, but the right bound\nis a property of the work: a persona that provisions a remote workspace\nwaits one to two minutes before it can do anything at all, while one that\nedits a file should never take that long. A persona that knows it is slow\nsays so here instead of every pod having to be reconfigured for it.\nAbsent means the delegation default. See [`crate::tools::sub_agent`].",
            "minimum": 0
          },
          "name": {
            "type": "string"
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "system_prompt": {
            "type": "string"
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "version": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional semantic version (e.g. \"1.1.0\") for built-in/seed personas.\nDrives force-upgrade on startup: when a bundled seed persona's version\nis higher than the installed copy's (a missing version counts as 0),\nthe installed file is overwritten. User-created personas omit this and\nare never force-upgraded. See `seed::write_versioned_seeds`."
          }
        }
      },
      "PersonaFallback": {
        "type": "object",
        "description": "One agent whose persona was withdrawn by an update.",
        "required": [
          "instance",
          "name",
          "from",
          "to"
        ],
        "properties": {
          "from": {
            "type": "string",
            "description": "The persona the new version no longer provides."
          },
          "instance": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "to": {
            "type": "string",
            "description": "The preset's new default, which it now uses instead."
          }
        }
      },
      "PersonaRole": {
        "type": "string",
        "description": "What a persona is allowed to do inside a preset.",
        "enum": [
          "default",
          "subagent",
          "internal"
        ]
      },
      "PersonaSummary": {
        "type": "object",
        "required": [
          "slug",
          "name",
          "description"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when this persona is provided by an enabled integration.\nLocal (user-owned) personas omit this."
          },
          "read_only": {
            "type": "boolean",
            "description": "True for pack-provided personas — the workshop disables Save/Delete."
          },
          "slug": {
            "type": "string"
          }
        }
      },
      "PlanStep": {
        "type": "object",
        "required": [
          "step"
        ],
        "properties": {
          "persona": {
            "type": [
              "string",
              "null"
            ],
            "description": "The persona this step is meant for. Advisory — the gate never checks that\nthe delegation actually went there, because a plan that reroutes mid-turn\nis a plan working as intended."
          },
          "status": {
            "$ref": "#/components/schemas/StepStatus"
          },
          "step": {
            "type": "string",
            "description": "What this step does, in one line, concrete enough to tell whether it happened."
          }
        }
      },
      "PodLayout": {
        "type": "object",
        "required": [
          "data_dir",
          "personas_dir",
          "skills_dir",
          "flows_dir",
          "sessions_dir",
          "api_tools_dir",
          "agent_presets_dir",
          "agent_instances_dir"
        ],
        "properties": {
          "agent_instances_dir": {
            "type": "string"
          },
          "agent_presets_dir": {
            "type": "string"
          },
          "api_tools_dir": {
            "type": "string"
          },
          "data_dir": {
            "type": "string"
          },
          "flows_dir": {
            "type": "string"
          },
          "personas_dir": {
            "type": "string"
          },
          "sessions_dir": {
            "type": "string"
          },
          "skills_dir": {
            "type": "string"
          }
        }
      },
      "PodSettings": {
        "type": "object",
        "description": "Everything a pod is configured to prefer.\n\nEvery field optional and every absence meaningful, because this file is\nwritten by whichever client last saved and read by a pod that may be newer\nthan it: an unknown field must not become an opinion.",
        "properties": {
          "timezone": {
            "type": [
              "string",
              "null"
            ],
            "description": "IANA timezone this pod's people live in — `\"America/Detroit\"`.\n\nUsed for a cron schedule that names no zone of its own. `None` falls back\nto the host clock, which is what every pod did before this existed and is\nalmost never what anybody meant."
          }
        }
      },
      "PodSnapshot": {
        "type": "object",
        "required": [
          "personas",
          "skills",
          "flows",
          "sessions",
          "api_tools",
          "keys",
          "agent_presets",
          "agent_instances",
          "default_agent_preset",
          "layout"
        ],
        "properties": {
          "agent_instances": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentInstance"
            },
            "description": "Agents that actually exist. Ephemeral ones are excluded — an unfiltered list\nis one row per chat ever started, which is noise, not information."
          },
          "agent_presets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PresetSummary"
            },
            "description": "Agents this pod can be. Both Workshop clients paint their agent picker from\nthe snapshot, so leaving these out costs an extra round-trip on every load."
          },
          "api_tools": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ApiToolSummary"
            }
          },
          "default_agent_preset": {
            "type": "string"
          },
          "flows": {
            "type": "array",
            "items": {
              "type": "object"
            },
            "description": "From the external `metalcraft-flows` crate, which has no `ToSchema`; the\nweb app doesn't consume this field, so expose it as an opaque object array."
          },
          "keys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/KeySummary"
            }
          },
          "layout": {
            "$ref": "#/components/schemas/PodLayout"
          },
          "personas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PersonaSummary"
            }
          },
          "sessions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DiagnosticsSessionSummary"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SkillSummary"
            }
          }
        }
      },
      "PresetDetail": {
        "type": "object",
        "description": "A preset with its roster resolved against what this pod actually has.\n\nTyped rather than assembled ad hoc so it reaches `openapi.json`: both Workshop\nfrontends generate their client types from the spec, and a response described only\nby a prose `description` gives them nothing to generate.",
        "required": [
          "preset",
          "personas"
        ],
        "properties": {
          "personas": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RosterPersona"
            }
          },
          "preset": {
            "$ref": "#/components/schemas/AgentPreset"
          }
        }
      },
      "PresetPersona": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "role": {
            "$ref": "#/components/schemas/PersonaRole"
          },
          "slug": {
            "type": "string"
          }
        }
      },
      "PresetSummary": {
        "type": "object",
        "required": [
          "slug",
          "name",
          "description",
          "default_persona",
          "persona_count"
        ],
        "properties": {
          "default_persona": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "library": {
            "type": "boolean",
            "description": "Not startable — see [`AgentPreset::library`]. Summaries still carry library\npresets (installing over one is still a slug collision, and the UI has a\nreason to show what a pack brought); a picker filters on this."
          },
          "name": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "persona_count": {
            "type": "integer",
            "minimum": 0
          },
          "read_only": {
            "type": "boolean"
          },
          "slug": {
            "type": "string"
          },
          "tagline": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "PreviewScheduleRequest": {
        "type": "object",
        "required": [
          "schedule"
        ],
        "properties": {
          "schedule": {
            "$ref": "#/components/schemas/ScheduleSpecDoc",
            "description": "The trigger to project. Need not correspond to anything saved."
          }
        }
      },
      "Progress": {
        "type": "object",
        "description": "Checked and total steps in the scratchpad's plan.",
        "required": [
          "done",
          "total"
        ],
        "properties": {
          "done": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "total": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "Project": {
        "type": "object",
        "description": "One project.",
        "required": [
          "id",
          "title",
          "goal",
          "kind",
          "instance_id",
          "agent_preset",
          "status",
          "created_at"
        ],
        "properties": {
          "agent_preset": {
            "type": "string"
          },
          "blocked_reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why it stopped, when it is blocked — the question to put to a person."
          },
          "conductor_instance_id": {
            "type": "string",
            "description": "The **conductor**: the small agent that *is* the project — it holds the\nplan, judges whether the goal is met, and writes each tick's briefing.\nSeparate from the worker so that the thing which says \"done\" is not the\nthing that wants to be finished.\n\nEmpty on a project created before the conductor existed, which falls back\nto sharing the worker's instance rather than losing its memory."
          },
          "counters": {
            "$ref": "#/components/schemas/Counters"
          },
          "created_at": {
            "type": "string"
          },
          "goal": {
            "type": "string",
            "description": "**The goal.** The one field that matters; everything else is\nconfiguration or a counter.\n\nA field rather than the project's identity, so it can be re-aimed: edit\nit and the project keeps its instances, memory, session, tasks,\nworkspace and history. Naming the container after this string is what\nthe old `Goal` type got wrong."
          },
          "heartbeat": {
            "$ref": "#/components/schemas/Heartbeat"
          },
          "id": {
            "type": "string"
          },
          "instance_id": {
            "type": "string",
            "description": "The **worker**: the agent that does the work, in a fresh context each\ntick. Owns the scratchpad and produces the evidence."
          },
          "io": {
            "$ref": "#/components/schemas/IoBinding",
            "description": "Where a question or a report is delivered. Reuses the binding scheduled\nfollow-ups already use, because it is the same problem: a job that\nfinishes when nobody is looking still has to reach somebody."
          },
          "journal_chat_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The chat that holds this project's journal — one line per tick, and where a\nperson answers to unblock it."
          },
          "kind": {
            "$ref": "#/components/schemas/ProjectKind"
          },
          "models": {
            "$ref": "#/components/schemas/ModelTiers"
          },
          "pending_run": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PendingRun"
              }
            ]
          },
          "rails": {
            "$ref": "#/components/schemas/Rails"
          },
          "session_id": {
            "type": "string",
            "description": "The conversation this project records itself into — one thread for its\nwhole life, which is what a person opens to see how it got here.\n\nA window, not a chat: nothing routes typing into it. Empty for a project\nmade before it existed, and for one whose first tick has not run yet."
          },
          "status": {
            "$ref": "#/components/schemas/ProjectStatus"
          },
          "tick_requested": {
            "type": "boolean",
            "description": "A person asked for a tick now, rather than at the next heartbeat.\n\nA flag rather than \"clear the bookmark\", because a force is a *request*\nand not a preemption: if a tick is already running, the running one\ncleared this at its start, so a force raised while it works survives and\nis honoured when it ends. Two turns of the same worker running at once is\nthe one thing a project must never do."
          },
          "title": {
            "type": "string",
            "description": "Short human handle, for lists."
          },
          "updated_at": {
            "type": "string"
          },
          "worker_brief": {
            "type": "string",
            "description": "The worker's own instructions, written once by the conductor from the\ngoal and stored here.\n\nWritten once rather than re-derived every tick, and that is the whole\ndifference between a brief and a briefing: this says what kind of work\nthis project *is* — the repo, the stack, the conventions, what \"careful\"\nmeans here — and it does not change when the plan does. It goes in the\nworker's system prompt; what changes each tick is the conductor's\nbriefing, and that goes in the message.\n\nEmpty until the first tick composes it, and empty is fine: the worker\nfalls back to its persona alone, which is exactly how it behaved before\nbriefs existed."
          },
          "workspace": {
            "$ref": "#/components/schemas/Workspace"
          }
        }
      },
      "ProjectDetail": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ProjectRow"
          },
          {
            "type": "object",
            "required": [
              "project_record",
              "scratchpad",
              "needs_groom",
              "tasks"
            ],
            "properties": {
              "needs_groom": {
                "type": "boolean",
                "description": "Set when the document has decayed enough that the next review tick will\ngroom it rather than merely audit the plan."
              },
              "project_record": {
                "$ref": "#/components/schemas/Project"
              },
              "scratchpad": {
                "type": "string",
                "description": "The scratchpad as written — the project's whole memory, verbatim."
              },
              "tasks": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ProjectTask"
                },
                "description": "The plan, as records. Empty for a project created before tasks existed —\nits plan is still checkboxes inside `scratchpad`."
              }
            }
          }
        ]
      },
      "ProjectFindings": {
        "type": "object",
        "required": [
          "findings",
          "open_prs",
          "max_open_prs"
        ],
        "properties": {
          "findings": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Finding"
            }
          },
          "max_open_prs": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "open_prs": {
            "type": "integer",
            "description": "How many PRs this project has open, against its own limit — the pair a\nreviewer needs to know whether it is holding back on purpose.",
            "minimum": 0
          }
        }
      },
      "ProjectJournal": {
        "type": "object",
        "required": [
          "entries"
        ],
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/JournalEntry"
            }
          }
        }
      },
      "ProjectKind": {
        "type": "string",
        "description": "What a project is trying to do, which selects its persona and its tick frame.",
        "enum": [
          "build",
          "audit"
        ]
      },
      "ProjectList": {
        "type": "object",
        "required": [
          "projects",
          "active",
          "max_active"
        ],
        "properties": {
          "active": {
            "type": "integer",
            "description": "How many are ticking, against the ceiling — what a \"new project\" button\nneeds to know before it is pressed.",
            "minimum": 0
          },
          "max_active": {
            "type": "integer",
            "minimum": 0
          },
          "projects": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProjectRow"
            }
          }
        }
      },
      "ProjectRepo": {
        "type": "object",
        "description": "One repo the project works in, inside its buildr.space workspace.",
        "required": [
          "full_name"
        ],
        "properties": {
          "branch": {
            "type": [
              "string",
              "null"
            ],
            "description": "The branch the project works on — `project/<slug>`, not the repo's default."
          },
          "dir": {
            "type": [
              "string",
              "null"
            ],
            "description": "Directory under `/workspace`. Absent means buildr's own default (the\nrepo's bare name)."
          },
          "full_name": {
            "type": "string",
            "description": "`owner/name` on GitHub."
          }
        }
      },
      "ProjectRole": {
        "type": "string",
        "description": "Which of a project's two agents an instance is.",
        "enum": [
          "worker",
          "conductor"
        ]
      },
      "ProjectRow": {
        "type": "object",
        "required": [
          "id",
          "title",
          "goal",
          "kind",
          "status",
          "instance_id",
          "progress",
          "ticks",
          "every_minutes",
          "created_at"
        ],
        "properties": {
          "blocked_reason": {
            "type": [
              "string",
              "null"
            ]
          },
          "created_at": {
            "type": "string"
          },
          "every_minutes": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "goal": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "instance_id": {
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ProjectKind"
          },
          "last_tick_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "next_tick_at": {
            "type": [
              "string",
              "null"
            ],
            "description": "When the next tick is owed, absent for a project that is not ticking."
          },
          "progress": {
            "$ref": "#/components/schemas/Progress",
            "description": "Checked/total plan steps, so a client draws a progress bar without\nparsing markdown."
          },
          "status": {
            "$ref": "#/components/schemas/ProjectStatus"
          },
          "ticks": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ProjectStatus": {
        "type": "string",
        "description": "Where a project is in its life.\n\n`Blocked` is the one that carries weight: it is where *every* stopping\ncondition lands — the agent asking a question, and every rail in [`Rails`]\ntripping. A project that quietly gave up would be indistinguishable from one\nstill working, which is the worst confusion this design could offer.",
        "enum": [
          "active",
          "blocked",
          "paused",
          "done",
          "failed"
        ]
      },
      "ProjectTask": {
        "type": "object",
        "description": "One slice of a project's plan.\n\nPublished as `ProjectTask` rather than `Task`. In Rust, inside this module,\n`Task` is unambiguous; on the wire it is not — a generated Swift client puts\nevery schema in one namespace, where a type called `Task` shadows Swift's own\n`Task` and breaks every `Task { … }` in the app. The name is only free here\nbecause this module is small.",
        "required": [
          "id",
          "title",
          "status",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "assignee": {
            "type": [
              "string",
              "null"
            ],
            "description": "Persona to delegate this to. `None` means the tick does it itself."
          },
          "attempts": {
            "type": "integer",
            "format": "int32",
            "description": "How many times this task has been attempted and come back unfinished.",
            "minimum": 0
          },
          "blocked_reason": {
            "type": [
              "string",
              "null"
            ]
          },
          "created_at": {
            "type": "string"
          },
          "deps": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Task ids **within this project** that must land first."
          },
          "detail": {
            "type": "string",
            "description": "What a delegate is handed when this task is dispatched. A delegate has\nnone of the project's context, so this carries every decision it depends on\n— the same contract a sub-agent's `task` argument has always had."
          },
          "evidence": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Evidence"
            }
          },
          "gate": {
            "type": [
              "string",
              "null"
            ],
            "description": "A command that must exit 0 before this task may be `Done`."
          },
          "id": {
            "type": "string",
            "description": "Short and typeable (`t1`, `t2`) — it is quoted in the scratchpad, in\nhandoffs and in the journal, so it has to survive being retyped."
          },
          "mutates_workspace": {
            "type": "boolean",
            "description": "Whether running this task writes to the project's workspace.\n\n**Defaults to `true`**, and the asymmetry is deliberate: a task wrongly\nmarked read-only can be dispatched alongside another that is editing the\nsame sprite, and two agents writing one workspace corrupt each other\nsilently. A task wrongly marked as mutating merely runs on its own. The\ncheap mistake is the safe one."
          },
          "pending_run": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PendingRun",
                "description": "A long-running command this task started and did not wait for. The\npre-flight reads it on the next tick, spending nothing."
              }
            ]
          },
          "status": {
            "$ref": "#/components/schemas/TaskStatus"
          },
          "title": {
            "type": "string",
            "description": "One line. This is what the rendered plan shows and what the UI lists."
          },
          "updated_at": {
            "type": "string"
          }
        }
      },
      "Provides": {
        "type": "object",
        "properties": {
          "integrations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/IntegrationRef"
            },
            "description": "Reads `integration_packs` too — the pre-0.30 name."
          },
          "personas": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "Rails": {
        "type": "object",
        "description": "Everything that can stop a project.\n\nAll of them land in [`ProjectStatus::Blocked`] rather than ending the project:\nrunning out of rope is a reason to ask a person, not to disappear.",
        "properties": {
          "compute_minutes_budget": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "buildr.space awake minutes this project may spend. Absent = only the\naccount's own plan bounds it.",
            "minimum": 0
          },
          "deadline": {
            "type": [
              "string",
              "null"
            ],
            "description": "RFC3339. Past it, the project blocks."
          },
          "max_consecutive_no_progress": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "max_open_prs": {
            "type": "integer",
            "format": "int32",
            "description": "How many of this project's PRs may be open at once. Twenty simultaneous bot\nPRs is how a repo learns to ignore them.",
            "minimum": 0
          },
          "max_ticks": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "RecommendedKey": {
        "type": "object",
        "description": "A key recommended by one or more *enabled* integrations (from their\n`requires_env`), with whether it currently resolves (key store or env) and\nwhich packs declare it. Drives the \"keys these packs still need\" list in\nthe key store UI — `configured: false` is the hint to add it.",
        "required": [
          "name",
          "configured",
          "packs",
          "managed"
        ],
        "properties": {
          "configured": {
            "type": "boolean"
          },
          "managed": {
            "type": "boolean",
            "description": "Platform-managed (env-authoritative, e.g. `METALCRAFT_TOKEN` injected into a\nprovisioned pod). The UI should show it as provided/read-only rather than\nprompting the user to paste a value."
          },
          "name": {
            "type": "string"
          },
          "packs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "Registries": {
        "type": "object",
        "description": "Where this pod is willing to fetch an agent pack from.\n\nReturned rather than only enforced so a UI can say what it accepts *before* the\nuser pastes a link and gets refused — and so a picker can offer the configured\nhosts by name rather than making somebody remember an origin.",
        "required": [
          "origins",
          "default",
          "registries"
        ],
        "properties": {
          "default": {
            "type": "string"
          },
          "origins": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Bare origins, kept for clients written against the earlier shape."
          },
          "registries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RegistryView"
            }
          }
        }
      },
      "RegistryView": {
        "type": "object",
        "required": [
          "name",
          "url",
          "trust",
          "is_default"
        ],
        "properties": {
          "is_default": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          },
          "trust": {
            "$ref": "#/components/schemas/Trust"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "Requires": {
        "type": "object",
        "description": "The dependencies a flow declares.",
        "properties": {
          "packs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PackRequirement"
            },
            "description": "Integration packs this flow needs, each with an optional version/hash\ncontract."
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Flat tool names the flow invokes (the real API surface — `tool` nodes bind\nby bare name). Auto-derived by [`derive_requires`]."
          }
        }
      },
      "ResetFailure": {
        "type": "object",
        "description": "A single entry the wipe could not remove.",
        "required": [
          "name",
          "error"
        ],
        "properties": {
          "error": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "description": "Entry name, relative to the data dir."
          }
        }
      },
      "ResetReport": {
        "type": "object",
        "description": "What the reset actually did — reported rather than assumed.\n\nA partial wipe is a real outcome (a busy file, a read-only mount, a\npermission the container lost), and it is the outcome where \"success\" is the\nmost dangerous thing to say: the operator would go on to test onboarding\nagainst a pod that still has half its state.",
        "required": [
          "scope",
          "data_dir",
          "removed",
          "kept",
          "failed",
          "restart"
        ],
        "properties": {
          "data_dir": {
            "type": "string",
            "description": "The data directory that was emptied."
          },
          "failed": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ResetFailure"
            },
            "description": "Entries that could not be removed. Non-empty means the pod is *not*\nfactory-fresh, whatever the HTTP status said."
          },
          "kept": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Entry names deliberately left in place by the scope."
          },
          "removed": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Entry names removed, sorted. Directory entries count once."
          },
          "restart": {
            "$ref": "#/components/schemas/RestartExpectation",
            "description": "Whether the process expects to be restarted for it, or left down."
          },
          "scope": {
            "$ref": "#/components/schemas/ResetScope"
          }
        }
      },
      "ResetScope": {
        "type": "string",
        "description": "How much of the pod to take away.",
        "enum": [
          "full",
          "keep_keys"
        ]
      },
      "RestartExpectation": {
        "type": "string",
        "description": "What the pod expects to happen to it after the wipe.",
        "enum": [
          "supervised",
          "manual"
        ]
      },
      "RestoreOutcome": {
        "type": "object",
        "required": [
          "kind",
          "name",
          "version",
          "status"
        ],
        "properties": {
          "detail": {
            "type": [
              "string",
              "null"
            ]
          },
          "kind": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "description": "`installed` | `failed`."
          },
          "version": {
            "type": "string"
          }
        }
      },
      "RestoreResult": {
        "type": "object",
        "description": "Wrapper for the lockfile-restore response (`{ outcomes: [...] }`).",
        "required": [
          "outcomes"
        ],
        "properties": {
          "outcomes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RestoreOutcome"
            }
          }
        }
      },
      "ResumeFlowRunRequest": {
        "type": "object",
        "required": [
          "handle"
        ],
        "properties": {
          "data": {
            "description": "Optional value to set as the resumed node's `_last` input."
          },
          "handle": {
            "type": "string",
            "description": "Handle to take (an approval decision, or `\"after\"` for a wait)."
          }
        }
      },
      "RosterPersona": {
        "type": "object",
        "description": "One entry in a preset's roster.\n\n`installed: false` is the interesting case: the preset names a persona this pod\ndoes not have. A UI should render it disabled with its `error` rather than\nsilently omit it — omission looks like the preset is smaller than it is.",
        "required": [
          "slug",
          "installed"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "error": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why it could not be resolved. Present only when `installed` is false."
          },
          "installed": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "slug": {
            "type": "string"
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "RunFlowOutput": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/FlowRunSummary"
          },
          {
            "$ref": "#/components/schemas/RunFlowResponse"
          }
        ],
        "description": "The run endpoint returns one of two shapes depending on the flow's spec\nversion: v2 (state-machine) flows return a [`FlowRunSummary`]; v1 (linear)\nflows return a [`RunFlowResponse`]. Documented as a `oneOf` so generated\nclients see a proper union rather than an opaque object. Never constructed —\nit exists only to describe the response schema."
      },
      "RunFlowRequest": {
        "type": "object",
        "properties": {
          "inputs": {
            "description": "Values for the flow's declared entry `inputs`. Object of `{ name: value }`;\nan input not named here falls back to its declared default, and a required\none with no default is left unset — a warning on the run rather than a\nrefusal to start it, so a flow can always be tried. Ignored by v1 flows."
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Run as this agent, so the run recalls from and writes to its memory and\nleaves a conversation behind. Omitted resolves to the flow's armed agent,\nor to the flow's own agent — see [`instance_for_manual_run`]."
          },
          "model_name": {
            "type": [
              "string",
              "null"
            ]
          },
          "persona_slug": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional persona override. A v2 flow owns its persona via the entry node,\nso this is normally omitted and left to the flow. It is only a fallback for\na flow that declares none (and for legacy v1 flows). Prompt nodes that\ndeclare their own persona override it regardless."
          }
        }
      },
      "RunFlowResponse": {
        "type": "object",
        "required": [
          "flow_id",
          "prompts"
        ],
        "properties": {
          "flow_id": {
            "type": "string"
          },
          "prompts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FlowPromptResult"
            }
          }
        }
      },
      "SavedFlow": {
        "type": "object",
        "description": "A persisted flow document — what a `.json` file on disk contains.\n\nA flow describes **what work is this**, and nothing else. When it runs, as\nwhom, and on which agent live in [`ScheduledFlow`](crate::scheduled::ScheduledFlow)\ndocuments that point at it by id. A flow with no `ScheduledFlow` pointing at\nit cannot fire — which is what makes \"installing something never starts\nbackground work\" a property of the format rather than a rule every install\npath has to remember.",
        "required": [
          "id",
          "name",
          "created_at",
          "updated_at",
          "flow"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "description": "ISO-8601 / RFC-3339 creation timestamp."
          },
          "flow": {
            "$ref": "#/components/schemas/FlowDefinition",
            "description": "The graph definition."
          },
          "id": {
            "type": "string",
            "description": "Stable identifier. Must match `^[A-Za-z0-9-]{1,64}$`."
          },
          "name": {
            "type": "string",
            "description": "Human-readable label."
          },
          "requires": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Requires",
                "description": "Declared integration-pack / tool dependencies. Absent on legacy documents;\nsee [`crate::requires`]."
              }
            ]
          },
          "spec_version": {
            "type": "string",
            "description": "Spec version this document conforms to. Defaults to `\"1\"` when absent."
          },
          "updated_at": {
            "type": "string",
            "description": "ISO-8601 / RFC-3339 last-modified timestamp."
          }
        }
      },
      "SchedulePreview": {
        "type": "object",
        "description": "Projected firing times for a trigger — `description` plus the next few runs.",
        "required": [
          "description",
          "next_runs"
        ],
        "properties": {
          "description": {
            "type": "string",
            "description": "Human-readable trigger, e.g. ``\"Cron `0 0 8 * * *` (America/Detroit)\"``."
          },
          "next_runs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Next few firing times as RFC-3339 strings. Empty for a manual trigger, and\nfor a cron this pod cannot parse — which is the visible symptom of a\nschedule that will never fire."
          }
        }
      },
      "ScheduleSpecDoc": {
        "type": "object",
        "description": "Wire shape of [`metalcraft_flows::ScheduleSpec`], for the document only. See\n[`ScheduledFlowDoc`] — same reason, same deal with drift.\n\nThe trigger is flattened onto the object and tagged by `type`, so a cron\nschedule is `{ \"type\": \"cron\", \"cron\": \"0 8 * * *\" }` rather than carrying a\nnested trigger object.",
        "required": [
          "type"
        ],
        "properties": {
          "cron": {
            "type": [
              "string",
              "null"
            ],
            "description": "Present for `cron`. A standard cron expression; the pod parses it, the\nspec crate does not."
          },
          "inputs": {
            "description": "Inputs handed to the flow when this fires, so one flow can run with\ndifferent parameters on different schedules."
          },
          "interval": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Present for `minutes` and `hours`. Must be positive.",
            "minimum": 0
          },
          "name": {
            "type": [
              "string",
              "null"
            ],
            "description": "Human-readable label (\"Morning brief\") — what a UI shows, and what the pod\nnames a minted agent after."
          },
          "persona": {
            "type": [
              "string",
              "null"
            ],
            "description": "Persona override for runs this schedule starts."
          },
          "timezone": {
            "type": [
              "string",
              "null"
            ],
            "description": "IANA timezone the `cron` trigger is evaluated in. Ignored by other\ntriggers, and `null` means the pod's own time."
          },
          "type": {
            "type": "string",
            "description": "`manual` | `minutes` | `hours` | `cron`."
          }
        }
      },
      "ScheduledFlowDoc": {
        "type": "object",
        "description": "What a [`metalcraft_flows::ScheduledFlow`] looks like on the wire, for the\nOpenAPI document only.\n\n**Nothing constructs one.** It exists because `ScheduledFlow` lives in the\n`metalcraft-flows` crate and does not derive `ToSchema`, so the only way to\ndescribe it here was `value_type = Object` — and an `Object` is not a gap in\nthe generated client, it is a hole that swallows its neighbours. utoipa emits\n`Record<string, never>` for it, and TypeScript intersects that index signature\nwith the fields beside it: `id: string` becomes `string & never`. Every client\nthat generates from this document lost `id`, `flow_id`, `enabled` and\n`schedule` — the whole stored half of every scheduled-flow response, including\nthe id needed to address one.\n\nA mirror rather than a `ToSchema` derive on the real type because the real type\nis in a published crate, and describing our own API should not require cutting\na release of somebody else's. The cost is that a mirror can drift from what it\nmirrors, so it does not get to: `scheduled_flow_schema_matches_the_artifact`\nserializes a real `ScheduledFlow` and fails if the two disagree about field\nnames. When `metalcraft-flows` next derives `ToSchema`, delete this and point\n`value_type` at the real thing.",
        "required": [
          "id",
          "flow_id",
          "enabled",
          "schedule",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "description": "RFC-3339 creation timestamp."
          },
          "enabled": {
            "type": "boolean",
            "description": "Whether it fires. The only switch there is — the flow no longer knows it is\nscheduled, so there is no flow-level master switch to disagree with."
          },
          "flow_id": {
            "type": "string",
            "description": "The flow this runs, by its `id`. May dangle: the flow can be deleted out\nfrom under it, which is why `flow_name` is optional on the row."
          },
          "from_suggestion": {
            "type": [
              "string",
              "null"
            ],
            "description": "The author suggestion this was created from. Provenance, not identity."
          },
          "id": {
            "type": "string",
            "description": "Stable identifier, opaque by convention (`sf_<random>`)."
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "The agent it runs as, so successive firings accumulate memory instead of\nwaking up amnesiac."
          },
          "schedule": {
            "$ref": "#/components/schemas/ScheduleSpecDoc",
            "description": "The trigger and its per-schedule overrides."
          },
          "updated_at": {
            "type": "string",
            "description": "RFC-3339 last-modified timestamp."
          }
        }
      },
      "ScheduledFlowList": {
        "type": "object",
        "required": [
          "scheduled"
        ],
        "properties": {
          "scheduled": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ScheduledFlowRow"
            }
          }
        }
      },
      "ScheduledFlowRow": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ScheduledFlowDoc",
            "description": "The artifact exactly as stored, so an editor can round-trip it without a\nsecond representation of the same thing."
          },
          {
            "type": "object",
            "required": [
              "description"
            ],
            "properties": {
              "description": {
                "type": "string",
                "description": "Human-readable trigger: ``\"Cron `0 0 8 * * *` (America/Detroit)\"``."
              },
              "flow_name": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Name of the flow it runs. **Absent when that flow no longer exists** — a\nschedule that can never fire, which is worth showing as broken rather than\nquietly listing as fine."
              },
              "instance_name": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Name of the agent it runs as. Absent if the instance was deleted out from\nunder it."
              },
              "next_fire_at": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Next projected fire time, or absent for a manual trigger — and for a cron\nthis pod cannot parse, which is how a schedule that will never fire looks."
              }
            }
          }
        ],
        "description": "One scheduled flow, resolved for display: the stored document plus the three\nthings a client would otherwise have to compute or fetch."
      },
      "SearchHit": {
        "type": "object",
        "description": "One result from a host's `/search`.\n\nEverything past the name is optional because a fetch-only host is a real\ndeployment rather than a degenerate one (§11.1 makes `/search` optional in the\nfirst place). A browse list renders what it got; it does not refuse what it\ndidn't.",
        "required": [
          "reference",
          "id",
          "name",
          "tags",
          "verified"
        ],
        "properties": {
          "avatar_url": {
            "type": [
              "string",
              "null"
            ]
          },
          "category": {
            "type": [
              "string",
              "null"
            ]
          },
          "id": {
            "type": "string",
            "description": "The id on this host, without the `@`."
          },
          "install_count": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "name": {
            "type": "string"
          },
          "reference": {
            "type": "string",
            "description": "What to install: `axoniac:@amy_kitchen`. **Qualified, always** — an\nunqualified reference is an error the moment two configured hosts publish the\nsame id (§11.3), and a browse list is precisely where that collision surfaces."
          },
          "tagline": {
            "type": [
              "string",
              "null"
            ]
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "verified": {
            "type": "boolean",
            "description": "Whether the host vouches for it — worth exactly as much as the host's `trust`\nmakes it worth, and on a `verified-only` host it is what decides whether this\npod will install the pack at all."
          },
          "version": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "SetEnabledRequest": {
        "type": "object",
        "description": "Kept only so the retired endpoint below still documents the body clients used\nto send; the value is ignored.",
        "required": [
          "enabled"
        ],
        "properties": {
          "enabled": {
            "type": "boolean"
          }
        }
      },
      "Severity": {
        "type": "string",
        "enum": [
          "high",
          "medium",
          "low"
        ]
      },
      "Skill": {
        "type": "object",
        "description": "A full skill (frontmatter description + body).",
        "required": [
          "slug",
          "description",
          "body"
        ],
        "properties": {
          "body": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ]
          },
          "read_only": {
            "type": "boolean"
          },
          "slug": {
            "type": "string"
          },
          "version": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional semantic version (e.g. \"1.1.0\") for built-in/seed skills, held\nin the frontmatter beside `description`. Drives force-upgrade on startup:\nwhen a bundled seed skill's version is higher than the installed copy's\n(a missing version counts as 0), the seed overwrites it. User skills\ncarrying no version are never force-upgraded. See\n`seed::write_versioned_seeds`."
          }
        }
      },
      "SkillSummary": {
        "type": "object",
        "description": "Summary of a skill for listings (no body).",
        "required": [
          "slug",
          "description"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "pack_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when this skill is provided by an enabled integration."
          },
          "read_only": {
            "type": "boolean",
            "description": "True for pack-provided skills — the workshop disables Save/Delete."
          },
          "slug": {
            "type": "string"
          }
        }
      },
      "StageReport": {
        "type": "object",
        "description": "What one stage did. `counts` is open-ended on purpose — each stage reports the\nnumbers that mean something for it, and a fixed struct would be mostly zeros.",
        "required": [
          "stage",
          "name",
          "ran",
          "counts",
          "millis"
        ],
        "properties": {
          "counts": {
            "type": "object"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          },
          "millis": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "name": {
            "type": "string"
          },
          "ran": {
            "type": "boolean",
            "description": "False when `MEMORY_DREAM_STAGES` excluded it."
          },
          "stage": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "StepStatus": {
        "type": "string",
        "enum": [
          "pending",
          "in_progress",
          "done",
          "skipped"
        ]
      },
      "TaskStatus": {
        "type": "string",
        "description": "Where a task has got to.\n\nThere is no `Ready` variant: readiness is *derived* from the dependency\ngraph ([`is_ready`]) rather than stored, so it can never disagree with the\ndeps it is supposed to summarise. Storing it would mean a promote pass, and\na promote pass means two sources of truth.",
        "enum": [
          "todo",
          "waiting",
          "blocked",
          "done",
          "dropped"
        ]
      },
      "TickKind": {
        "type": "string",
        "description": "What this tick is for. The kind picks the frame the agent is given and the\nmodel tier that runs it.",
        "enum": [
          "plan",
          "work",
          "review"
        ]
      },
      "TimelineEvent": {
        "type": "object",
        "required": [
          "kind",
          "file",
          "data"
        ],
        "properties": {
          "data": {},
          "file": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          }
        }
      },
      "TimezoneRegion": {
        "type": "object",
        "description": "One region's worth of timezones, for a picker that groups them.",
        "required": [
          "region",
          "zones"
        ],
        "properties": {
          "region": {
            "type": "string",
            "description": "The IANA area — `\"America\"`, `\"Europe\"` — or `\"UTC\"`."
          },
          "zones": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Full zone names in that area, sorted: `\"America/Detroit\"`."
          }
        }
      },
      "Trust": {
        "type": "string",
        "description": "How much a host's word is worth.",
        "enum": [
          "first-party",
          "verified-only",
          "explicit"
        ]
      },
      "UninstallReport": {
        "type": "object",
        "required": [
          "id",
          "orphaned_agents",
          "packs_freed"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "orphaned_agents": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Agents left pointing at a preset that no longer exists. Never silently\ndeleted — someone's memories are in there."
          },
          "packs_freed": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "UpdateChannelRequest": {
        "type": "object",
        "required": [
          "name",
          "url"
        ],
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          },
          "secret": {
            "type": [
              "string",
              "null"
            ],
            "description": "New secret; omit or leave empty to keep the existing one."
          },
          "url": {
            "type": "string"
          }
        }
      },
      "UpdateProjectRequest": {
        "type": "object",
        "properties": {
          "answer": {
            "type": [
              "string",
              "null"
            ],
            "description": "The answer to whatever it blocked on. Appended to the scratchpad's State,\nwhich is where the next tick will look for it."
          },
          "every_minutes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "minimum": 0
          },
          "goal": {
            "type": [
              "string",
              "null"
            ],
            "description": "Restate the project. Allowed on purpose — a project that turned out to be the\nwrong project is better corrected than abandoned — but it does not rewrite\nthe scratchpad, so the next tick will notice the two disagree."
          },
          "models": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ModelTiers"
              }
            ]
          },
          "rails": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/Rails"
              }
            ]
          },
          "status": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProjectStatus"
              }
            ]
          },
          "title": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "UpdateReport": {
        "type": "object",
        "description": "What an update did, beyond what the install did.",
        "required": [
          "id",
          "from_version",
          "to_version",
          "install",
          "personas_fell_back",
          "orphaned",
          "memory_bases_repointed"
        ],
        "properties": {
          "from_version": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "install": {
            "$ref": "#/components/schemas/InstallReport"
          },
          "memory_bases_repointed": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Agents whose shipped knowledge now points at the new version's base layer."
          },
          "orphaned": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Orphaned"
            },
            "description": "Live agents whose preset was withdrawn (§12.3)."
          },
          "personas_fell_back": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PersonaFallback"
            },
            "description": "Live agents whose persona was withdrawn and fell back (§12.3)."
          },
          "to_version": {
            "type": "string"
          }
        }
      },
      "UpdateScheduledFlowRequest": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "Pause (`false`) or resume (`true`) without deleting. The agent and its\nmemory are untouched either way."
          },
          "instance_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Move this schedule to a different agent."
          },
          "schedule": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ScheduleSpecDoc",
                "description": "Replace the trigger and its overrides."
              }
            ]
          }
        }
      },
      "Workspace": {
        "type": "object",
        "description": "The project's buildr.space workspace.\n\n**A cache, not state.** The truth is the branch on GitHub plus the\nscratchpad; this may be reaped, deleted after a week of hibernation on the\nfree plan, or thrown away because the sprite got into a bad state. A tick\nreconciles rather than assuming, and a project that cannot survive losing this\nis a project that dies over a weekend.",
        "properties": {
          "id": {
            "type": [
              "string",
              "null"
            ],
            "description": "buildr.space workspace id. `None` until the first tick provisions one."
          },
          "last_provisioned_at": {
            "type": [
              "string",
              "null"
            ]
          },
          "repos": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProjectRepo"
            }
          }
        }
      },
      "WriteScratchpadRequest": {
        "type": "object",
        "required": [
          "markdown"
        ],
        "properties": {
          "markdown": {
            "type": "string"
          }
        }
      },
      "WriteTasksRequest": {
        "type": "object",
        "required": [
          "tasks"
        ],
        "properties": {
          "tasks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProjectTask"
            },
            "description": "The whole list, replacing what is there. Ids are preserved as given, so\na client edits what it read rather than posting a diff."
          }
        }
      }
    },
    "securitySchemes": {
      "bearer": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "token"
      }
    }
  },
  "security": [
    {
      "bearer": []
    }
  ],
  "tags": [
    {
      "name": "agent",
      "description": "Agent identity + project snapshot"
    },
    {
      "name": "personas",
      "description": "Persona definitions"
    },
    {
      "name": "skills",
      "description": "Skill definitions"
    },
    {
      "name": "flows",
      "description": "Flows, flow runs, and flow templates"
    },
    {
      "name": "diagnostics",
      "description": "Diagnostics session browsing"
    },
    {
      "name": "api-tools",
      "description": "HTTP-API tool configs"
    },
    {
      "name": "keys",
      "description": "The agent key/secret store"
    },
    {
      "name": "settings",
      "description": "Pod-wide preferences, and the timezones they can name"
    },
    {
      "name": "chats",
      "description": "Interactive chat sessions"
    },
    {
      "name": "scheduled-tasks",
      "description": "Scheduled follow-ups"
    },
    {
      "name": "integrations",
      "description": "Installable integrations"
    },
    {
      "name": "agent-packs",
      "description": "Installable agent packs — an agent plus every persona, skill and integration it needs"
    },
    {
      "name": "agent-presets",
      "description": "Agents this pod can be — a default persona, its callable roster, and the skills and packs they need"
    },
    {
      "name": "agent-instances",
      "description": "Agents that exist — each with its own memory and conversations"
    },
    {
      "name": "projects",
      "description": "Long-running projects: what this pod is working towards on its own, and how far it has got"
    },
    {
      "name": "gateway",
      "description": "Messaging gateway channels + Metalcraft connect"
    }
  ]
}
