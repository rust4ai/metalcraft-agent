{
  "name": "mcode_list_dir",
  "description": "List a directory in the workspace (convenience alias of mcode_read_file for a folder). Pass path relative to the repo root; omit for the root. Returns { kind:'dir', entries:[{name,is_dir}] }.",
  "method": "GET",
  "url": "https://code.metalcraftai.com/api/v1/workspaces/{id}/files",
  "headers": {
    "Authorization": "Bearer $METALCRAFT_TOKEN",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Workspace id (UUID)."
      },
      "path": {
        "type": "string",
        "description": "Directory path relative to the repo root; omit for the root."
      }
    },
    "required": [
      "id"
    ]
  },
  "body_mapping": "none"
}
