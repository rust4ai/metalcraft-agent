{
  "name": "vestaloop_delete_event",
  "description": "Delete a calendar event by `id` (UUID from vestaloop_list_events). Requires a read/write key (403 otherwise). Also removes it from the member's linked Google calendar. This is irreversible — confirm the exact event (title and time) with the user before deleting. Returns 204 No Content on success.",
  "method": "DELETE",
  "url": "https://vestaloop.com/api/v1/events/{id}",
  "headers": {
    "Authorization": "Bearer $VESTALOOP_API_KEY",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "id": { "type": "string", "description": "Event id (UUID) to delete." }
    },
    "required": ["id"]
  },
  "body_mapping": "none"
}
