{
  "name": "vestaloop_get_event",
  "description": "Get one calendar event by its `id` (a UUID obtained from vestaloop_list_events). Returns the full event object (title, description, location, starts_at, ends_at, all_day, scope, source, status). Fetch the current event with this before a partial update, since vestaloop_update_event replaces all fields.",
  "method": "GET",
  "url": "https://vestaloop.com/api/v1/events/{id}",
  "headers": {
    "Authorization": "Bearer $VESTALOOP_API_KEY",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "id": { "type": "string", "description": "Event id (UUID) from vestaloop_list_events." }
    },
    "required": ["id"]
  },
  "body_mapping": "none"
}
