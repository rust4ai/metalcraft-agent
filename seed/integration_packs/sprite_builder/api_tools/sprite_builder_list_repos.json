{
  "name": "sprite_builder_list_repos",
  "description": "List the GitHub repos the API key owner can access (via their GitHub OAuth grant). Use to find a repo_full_name (owner/repo) before creating a project.",
  "method": "GET",
  "url": "$SPRITE_BUILDER_BASE_URL/api/repos",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {},
    "required": []
  },
  "body_mapping": "none"
}
