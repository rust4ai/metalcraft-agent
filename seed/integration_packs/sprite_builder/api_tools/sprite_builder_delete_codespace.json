{
  "name": "sprite_builder_delete_codespace",
  "description": "Tear down a codespace and its sprite. Irreversible \u2014 confirm with the user first unless they asked.",
  "method": "DELETE",
  "url": "$SPRITE_BUILDER_BASE_URL/api/codespaces/{codespace_id}",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "codespace_id": {
        "type": "string",
        "description": "The codespace's id (UUID, from sprite_builder_create_codespace / sprite_builder_list_codespaces)."
      }
    },
    "required": [
      "codespace_id"
    ]
  },
  "body_mapping": "none"
}
