{
  "name": "sprite_builder_codespace_delete_path",
  "description": "Delete a file or directory in the codespace workspace. Path relative to /workspace/app; cannot be the root.",
  "method": "DELETE",
  "url": "$SPRITE_BUILDER_BASE_URL/api/codespaces/{codespace_id}/files?path={path}",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "codespace_id": {
        "type": "string",
        "description": "The codespace's id (UUID, from sprite_builder_create_codespace / sprite_builder_list_codespaces)."
      },
      "path": {
        "type": "string",
        "description": "Path relative to /workspace/app to delete."
      }
    },
    "required": [
      "codespace_id",
      "path"
    ]
  },
  "body_mapping": "none"
}
