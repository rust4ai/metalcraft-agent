{
  "name": "sprite_builder_codespace_git",
  "description": "Run a git operation in the codespace working tree. op is one of status | diff | commit | push | pull. message is required for commit. push/pull authenticate with the owner's GitHub token. Returns {op, output, exit_code}.",
  "method": "POST",
  "url": "$SPRITE_BUILDER_BASE_URL/api/codespaces/{codespace_id}/git",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "codespace_id": {
        "type": "string",
        "description": "The codespace's id (UUID, from sprite_builder_create_codespace / sprite_builder_list_codespaces)."
      },
      "op": {
        "type": "string",
        "description": "Git operation.",
        "enum": [
          "status",
          "diff",
          "commit",
          "push",
          "pull"
        ]
      },
      "message": {
        "type": "string",
        "description": "Commit message (required when op='commit')."
      }
    },
    "required": [
      "codespace_id",
      "op"
    ]
  },
  "body_mapping": "params"
}
