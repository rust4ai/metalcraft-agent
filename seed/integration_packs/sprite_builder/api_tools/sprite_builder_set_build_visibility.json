{
  "name": "sprite_builder_set_build_visibility",
  "description": "Set whether a deployed build's public URL is publicly accessible. public=true makes it open; public=false requires auth. The build must have a deployment (a sprite).",
  "method": "POST",
  "url": "$SPRITE_BUILDER_BASE_URL/api/builds/{build_id}/url-visibility",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "build_id": {
        "type": "string",
        "description": "The build's id (UUID, from sprite_builder_create_build / sprite_builder_list_builds)."
      },
      "public": {
        "type": "boolean",
        "description": "true = public URL, false = private/auth-gated."
      }
    },
    "required": [
      "build_id",
      "public"
    ]
  },
  "body_mapping": "params"
}
