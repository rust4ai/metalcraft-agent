{
  "name": "sprite_builder_list_projects",
  "description": "List the Sprite Builder projects owned by the API key. Each project is a GitHub repo plus build settings; returns id (UUID), name, repo_full_name, default_branch, dockerfile_path, container_port. Start here to discover project ids.",
  "method": "GET",
  "url": "$SPRITE_BUILDER_BASE_URL/api/projects",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {},
    "required": []
  },
  "body_mapping": "none"
}
