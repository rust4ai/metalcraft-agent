{
  "name": "sprite_builder_docuspace_create_folder",
  "description": "Create an empty folder in a docuspace (writes a .keep marker). Only needed to make a folder visible before it has files \u2014 folders are otherwise implicit.",
  "method": "POST",
  "url": "$SPRITE_BUILDER_BASE_URL/api/docuspaces/{docuspace_id}/folders",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "docuspace_id": {
        "type": "string",
        "description": "The docuspace's id (UUID, from sprite_builder_create_docuspace / sprite_builder_list_docuspaces)."
      },
      "path": {
        "type": "string",
        "description": "Folder path to create."
      }
    },
    "required": [
      "docuspace_id",
      "path"
    ]
  },
  "body_mapping": "params"
}
