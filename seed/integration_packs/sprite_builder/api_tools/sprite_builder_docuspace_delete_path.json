{
  "name": "sprite_builder_docuspace_delete_path",
  "description": "Delete a file, or a folder and everything under it, in a docuspace.",
  "method": "DELETE",
  "url": "$SPRITE_BUILDER_BASE_URL/api/docuspaces/{docuspace_id}/files?path={path}",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "docuspace_id": {
        "type": "string",
        "description": "The docuspace's id (UUID, from sprite_builder_create_docuspace / sprite_builder_list_docuspaces)."
      },
      "path": {
        "type": "string",
        "description": "Path within the docuspace to delete."
      }
    },
    "required": [
      "docuspace_id",
      "path"
    ]
  },
  "body_mapping": "none"
}
