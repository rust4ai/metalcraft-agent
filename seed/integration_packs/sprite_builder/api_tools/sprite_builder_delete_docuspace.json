{
  "name": "sprite_builder_delete_docuspace",
  "description": "Delete a docuspace and all of its stored objects. Irreversible \u2014 confirm with the user first unless they asked.",
  "method": "DELETE",
  "url": "$SPRITE_BUILDER_BASE_URL/api/docuspaces/{docuspace_id}",
  "headers": {
    "Authorization": "Bearer $SPRITE_BUILDER_API_KEY"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "docuspace_id": {
        "type": "string",
        "description": "The docuspace's id (UUID, from sprite_builder_create_docuspace / sprite_builder_list_docuspaces)."
      }
    },
    "required": [
      "docuspace_id"
    ]
  },
  "body_mapping": "none"
}
