{
  "name": "mcal_whoami",
  "description": "Validate the METALCRAFT_TOKEN and see its scope. Returns { sub, email, scopes } where `scopes` is a list like [\"calendar:read\",\"calendar:write\"] (or null for a full-access owner session). Call this first to confirm the token works and whether it can write (create/update/delete need calendar:write). Takes no parameters.",
  "method": "GET",
  "url": "https://calendar.metalcraftai.com/api/v1/whoami",
  "headers": {
    "Authorization": "Bearer $METALCRAFT_TOKEN",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {}
  },
  "body_mapping": "none"
}
