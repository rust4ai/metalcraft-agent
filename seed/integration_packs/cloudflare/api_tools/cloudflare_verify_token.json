{
  "name": "cloudflare_verify_token",
  "description": "Verify the configured CLOUDFLARE_API_TOKEN is valid and return its status. The cheapest way to confirm the token works before doing DNS work — a successful result has result.status == 'active'. Takes no parameters.",
  "method": "GET",
  "url": "https://api.cloudflare.com/client/v4/user/tokens/verify",
  "headers": {
    "Authorization": "Bearer $CLOUDFLARE_API_TOKEN",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {}
  },
  "body_mapping": "none"
}
