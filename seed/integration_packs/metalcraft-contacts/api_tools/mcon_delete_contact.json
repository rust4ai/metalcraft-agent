{
  "name": "mcon_delete_contact",
  "description": "Delete a contact by `slug` (also removes its photo). This is permanent — confirm the exact contact (display_name via mcon_get_contact) before calling. Requires the write scope (403 otherwise). Returns no content on success.",
  "method": "DELETE",
  "url": "https://contacts.metalcraftai.com/api/v1/contacts/{slug}",
  "headers": { "Authorization": "Bearer $METALCRAFT_TOKEN", "Accept": "application/json" },
  "parameters": {
    "type": "object",
    "properties": {
      "slug": { "type": "string", "description": "Contact slug to delete." }
    },
    "required": ["slug"]
  },
  "body_mapping": "none"
}
