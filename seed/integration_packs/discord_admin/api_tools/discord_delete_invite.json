{
  "name": "discord_delete_invite",
  "description": "Revoke an invite by its code. Provide `invite_code` (the short code, not the full URL). Requires MANAGE_CHANNELS on the invite's channel or MANAGE_GUILD.",
  "method": "DELETE",
  "url": "https://discord.com/api/v10/invites/{invite_code}",
  "headers": {
    "Authorization": "Bot $DISCORD_BOT_TOKEN",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "invite_code": {
        "type": "string",
        "description": "The invite code (the part after discord.gg/)."
      }
    },
    "required": [
      "invite_code"
    ]
  },
  "body_mapping": "none"
}
