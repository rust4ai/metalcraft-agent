{
  "name": "discord_list_guild_channels",
  "description": "List all channels in a guild (does not include threads). Returns each channel's id, name, type, position, parent_id, and permission_overwrites. Provide `guild_id`.",
  "method": "GET",
  "url": "https://discord.com/api/v10/guilds/{guild_id}/channels",
  "headers": {
    "Authorization": "Bot $DISCORD_BOT_TOKEN",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "guild_id": {
        "type": "string",
        "description": "The guild (server) ID (snowflake)."
      }
    },
    "required": [
      "guild_id"
    ]
  },
  "body_mapping": "none"
}
