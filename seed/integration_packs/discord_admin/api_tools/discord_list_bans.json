{
  "name": "discord_list_bans",
  "description": "List a guild's bans (each entry has the banned user and the ban reason). Provide `guild_id`; optional `limit` (1-1000, default 1000). Requires BAN_MEMBERS.",
  "method": "GET",
  "url": "https://discord.com/api/v10/guilds/{guild_id}/bans?limit={limit}",
  "headers": {
    "Authorization": "Bot $DISCORD_BOT_TOKEN",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "guild_id": {
        "type": "string",
        "description": "The guild (server) ID (snowflake)."
      },
      "limit": {
        "type": "integer",
        "description": "Max bans to return, 1-1000. Defaults to 1000."
      }
    },
    "required": [
      "guild_id"
    ]
  },
  "body_mapping": "none"
}
