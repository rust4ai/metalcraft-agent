{
  "name": "discord_create_ban",
  "description": "Ban a member from the guild. Provide `guild_id`, `user_id`; optional `delete_message_seconds` (0-604800, delete their recent messages up to 7 days). Requires BAN_MEMBERS.",
  "method": "PUT",
  "url": "https://discord.com/api/v10/guilds/{guild_id}/bans/{user_id}",
  "headers": {
    "Authorization": "Bot $DISCORD_BOT_TOKEN",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "guild_id": {
        "type": "string",
        "description": "The guild (server) ID (snowflake)."
      },
      "user_id": {
        "type": "string",
        "description": "The user ID (snowflake)."
      },
      "delete_message_seconds": {
        "type": "integer",
        "description": "Delete the user's messages from the last N seconds (0-604800 = up to 7 days)."
      }
    },
    "required": [
      "guild_id",
      "user_id"
    ]
  },
  "body_mapping": "params"
}
