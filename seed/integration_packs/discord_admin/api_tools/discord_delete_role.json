{
  "name": "discord_delete_role",
  "description": "Delete a role. Cannot be undone. Provide `guild_id` and `role_id`. Requires MANAGE_ROLES.",
  "method": "DELETE",
  "url": "https://discord.com/api/v10/guilds/{guild_id}/roles/{role_id}",
  "headers": {
    "Authorization": "Bot $DISCORD_BOT_TOKEN",
    "Content-Type": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "guild_id": {
        "type": "string",
        "description": "The guild (server) ID (snowflake)."
      },
      "role_id": {
        "type": "string",
        "description": "The role ID (snowflake)."
      }
    },
    "required": [
      "guild_id",
      "role_id"
    ]
  },
  "body_mapping": "none"
}
