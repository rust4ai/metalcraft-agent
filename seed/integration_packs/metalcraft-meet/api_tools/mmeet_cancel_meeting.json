{
  "name": "mmeet_cancel_meeting",
  "description": "Cancel a meeting by `slug` (soft-cancel: status becomes 'cancelled'). Removes the mirrored Metalcraft Calendar event. Requires the write scope (403 otherwise). Confirm the exact meeting (title + time) with the user before cancelling. Returns 204 No Content on success.",
  "method": "DELETE",
  "url": "https://meet.metalcraftai.com/api/v1/meetings/{slug}",
  "headers": { "Authorization": "Bearer $METALCRAFT_TOKEN", "Accept": "application/json" },
  "parameters": {
    "type": "object",
    "properties": {
      "slug": { "type": "string", "description": "Meeting slug to cancel." }
    },
    "required": ["slug"]
  },
  "body_mapping": "none"
}
