{
  "name": "mmeet_get_meeting",
  "description": "Get one meeting by `slug`, with its full participant list. Returns the meeting (slug, room_id, title, description, scheduled_start/end UTC ISO-8601, timezone, status, join_url, calendar_event_id) plus `participants` (email, display_name, role 'host'|'member'|'guest', rsvp).",
  "method": "GET",
  "url": "https://meet.metalcraftai.com/api/v1/meetings/{slug}",
  "headers": { "Authorization": "Bearer $METALCRAFT_TOKEN", "Accept": "application/json" },
  "parameters": {
    "type": "object",
    "properties": {
      "slug": { "type": "string", "description": "Meeting slug (from mmeet_list_meetings)." }
    },
    "required": ["slug"]
  },
  "body_mapping": "none"
}
