{
  "name": "mnote_create_category",
  "description": "Create a category (a color-coded tag), e.g. 'ideas'. `name` is the label; a unique color is assigned automatically from a fixed palette. Fails with 409 if the name is taken or the account already has 12 categories (the max). Requires the write scope (403 otherwise). Returns the created category including its id and color.",
  "method": "POST",
  "url": "https://notes.metalcraftai.com/api/v1/categories",
  "headers": { "Authorization": "Bearer $METALCRAFT_TOKEN", "Content-Type": "application/json", "Accept": "application/json" },
  "parameters": {
    "type": "object",
    "properties": {
      "name": { "type": "string", "description": "Category name (e.g. 'ideas')." }
    },
    "required": ["name"]
  },
  "body_mapping": "params"
}
