{
  "name": "mdrv_whoami",
  "description": "Validate the METALCRAFT_TOKEN and see its scope. Returns { sub, email, scopes } where `scopes` is a list like [\"read\",\"write\"] (or null for a full-access owner session). Call this first to confirm the token works and whether it can write (create/upload/move/delete need write). Takes no parameters.",
  "method": "GET",
  "url": "https://drive.metalcraftai.com/api/v1/whoami",
  "headers": {
    "Authorization": "Bearer $METALCRAFT_TOKEN",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {}
  },
  "body_mapping": "none"
}
