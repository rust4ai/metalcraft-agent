{
  "name": "mcode_hibernate_workspace",
  "description": "Hibernate a `ready` workspace (checkpoints its disk, stops routing work to it) to save resources when you're done for now. Wake it later with mcode_wake_workspace. Requires the write scope.",
  "method": "POST",
  "url": "https://code.metalcraftai.com/api/v1/workspaces/{id}/hibernate",
  "headers": {
    "Authorization": "Bearer $METALCRAFT_TOKEN",
    "Content-Type": "application/json",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Workspace id (UUID)."
      }
    },
    "required": [
      "id"
    ]
  },
  "body_mapping": "none"
}
