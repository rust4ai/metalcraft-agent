{
  "name": "mcal_remove_meeting",
  "description": "Remove the video meeting from an event by `id` in calendar `calendar` (tears down the Metalcraft Meet room and clears the join URL). Requires the write scope. Returns 204 No Content.",
  "method": "DELETE",
  "url": "https://calendar.metalcraftai.com/api/v1/calendars/{calendar}/events/{id}/meeting",
  "headers": {
    "Authorization": "Bearer $METALCRAFT_TOKEN",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "calendar": { "type": "string", "description": "Calendar slug." },
      "id": { "type": "string", "description": "Event id (UUID)." }
    },
    "required": ["calendar", "id"]
  },
  "body_mapping": "none"
}
