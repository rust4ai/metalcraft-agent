{
  "name": "memail_whoami",
  "description": "Validate the METALCRAFT_TOKEN and see its scope. Returns { user_id, email, scopes } where `scopes` is a list like [\"read\",\"write\"] (or null for a full-access owner session). Call this first to confirm the token works. Everything in this pack is read-only, so read access is all you need. Takes no parameters.",
  "method": "GET",
  "url": "https://email.metalcraftai.com/api/v1/whoami",
  "headers": {
    "Authorization": "Bearer $METALCRAFT_TOKEN",
    "Accept": "application/json"
  },
  "parameters": {
    "type": "object",
    "properties": {}
  },
  "body_mapping": "none"
}
